# Следующая сессия — старт отсюда

> **Дата:** 2026-06-03.
> Контекст сессии 2026-06-02 — в `ARCHITECTURE.md` (раздел «Сессия 2026-06-02») и
> в memory (`project_ip_pattern_audit`, `project_ip_model_routing`).

---

## Сделано 2026-06-02 (всё в проде + `main`)

Обогащение KB до **P-F1…P-F20 / P-M1…P-M11** (N-3/N-4/N-5: проктология, тазовое дно,
ЭД, функц.ЖКТ, щитовидка, воспаление) + свёрнуто в воркер + спец-вопросы анкеты.
Верификация первоисточников (правки PRIDE/Wu/рыбий жир/клетчатка/PFMT/ашваганда).
Тон-голос «забота» + приветствие/пожелание. Честность «не указано» (фронт `__touched`
→ воркер `SK()` + локальные карточки `seenF()`/bio-load «—»). Роутинг he/ar/ja/ko→
Sonnet 4.6, max_tokens 2600. BMR вычисляется (убраны вода/кость/BMR-инпут). Фикс
«мужчина·перименопауза». **Анализы УБРАНЫ из ИП** (→ELITE-анкета/нутрициолог).

---

## Задачи на завтра (по приоритету)

### 1. Контрольный прогон ИП (после сегодняшних правок) — 10 мин, браузер
- Пустой ввод (только вес/рост/возраст/талию): биозагрузка «—», нет выдуманных
  карточек «HRV в норме / дыхание 15», ИИ зовёт внести данные.
- Полный мужской и женский прогон: фаза верная (андропауза/пери), после «Цикла»
  идёт «Биоимпеданс»→«Динамика»→результат **без скачков нумерации** (анализы убраны).
- 1–2 языка для уверенности (en + he/ja/ko на Sonnet).

### 2. Биоимпеданс — подсказки-референсы к полям (Игорь просил)
Поля состава тела «голые» — можно ввести 1 или 100, непонятно. Добавить
placeholder/подсказку с типичным диапазоном (жир % по полу/возрасту, мышцы %,
висцеральный 1–12 норма, биовозраст). 3 файла (pro/pro-expert/elite) × 12 языков.

### 3. ELITE-анкета — графа «Анализы» (Игорь думает над форматом)
Анализы ушли из ИП → нужны нутрициологу в ведении. **Решение пользователя:** полную
анкету отправлять **после контакта/первого зума**, возможно **вручную**. Формат поля
(свободный текст vs структурные поля) — обсудить. onb_ анкета в elite/pro-expert.

### 4. Хвосты (по желанию)
- Белая вспышка при ЗАГРУЗКЕ страницы (3-4 раза; не на шагах) — косметика, нужен браузер.
- Дообогатить нутри-кластер Barbie Boules; верификация остальных глав N-1/N-2 при желании.
- Oura production OAuth (ждёт партнёрских кредов) — см. ARCHITECTURE §16.
- Косметика: устаревшее имя `proceedFromStep15` (работает); orphaned T-ключ `s12_badge`
  (анализы) в pro — мёртвый, можно убрать.

---

## Правила работы (важно)

- Никаких AskUserQuestion-опросников — одно конкретное предложение прозой + рекомендация.
- Короткие ответы (3–8 строк прозой), без таблиц/диаграмм без запроса.
- Approval per-file — не переносить паттерн с одного тарифа/файла на другой без «ок».
- **«Аккуратненько, ничего не поломать»**: при правке множества файлов — бэкапы +
  must-проверки + синтаксис-чек inline-JS (извлечь `<script>` → `node --check`) ДО коммита.
- Деплой воркера: `cd interpreter && wrangler deploy` (OAuth viaelcom@gmail.com).
  HTML-файлы — GitHub Pages (только пуш, без деплоя).
- Папки `Infa Cloude/N-*` — провенанс (gitignore, ~647МБ), не коммитить.
