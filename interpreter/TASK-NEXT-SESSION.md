# Следующая сессия — старт отсюда

> **Обновлено: 2026-06-04 (вечер).** Контекст — ARCHITECTURE.md (§18.10 ПОЛНЫЙ механизм
> анкеты программы+ELITE, §16 care-бот+FAQ-память, §17 канал ведения) и memory
> (`project_booking_system`, `project_vedenie_channel_target`, `project_ip_model_routing`).

---

## 🆕 СТАРТ НОВОГО ЧАТА — задача: засев FAQ-памяти «эталон»

**Тема нового чата:** наполнить FAQ-память care-бота базой выверенных пар «вопрос→ответ».

**Что уже готово (в проде, не трогать):** механизм FAQ-памяти — `faq:db` (KV `EXPERT_DRAFTS`),
кнопка «⭐ Сохранить в эталон», семантический поиск Haiku (ARCHITECTURE §16). Черновик
~30 Q→A лежит в `interpreter/faq-seed-draft.md` (помечен «не подтверждён»).

**Задача:**
1. Игорь пришлёт свой набор вопросов (и/или утвердит черновик `faq-seed-draft.md`).
2. Claude дорабатывает/структурирует Q→A на RU (база русскоязычная), темы — 4 программы +
   сквозные (сон, ЖКТ, гормоны, анализы, добавки, питание, вес, стресс). Цель — 400–500,
   начать с ~50 частых.
3. **Вычитка Maryna обязательна** — это медицина; бот лишь ПОДСКАЗЫВАЕТ нутрициологу, но
   засев должен быть выверен человеком (правило «только подтверждённые человеком ответы»).
4. Залить готовое в `faq:db` разовым сидером (как чинили webhook — временный guarded-роут
   в воркере, дёрнуть, удалить). Формат записи: `{q, a, ts}`, ≤500, один KV-ключ `faq:db`.

**Наставления (важно):**
- Минимальные точечные правки; деплой воркера — `cd interpreter && wrangler deploy`; HTML —
  `git push` (GitHub Pages, via-l.com). Коммит/деплой — по ходу, но push — по слову Игоря.
- «Аккуратненько»: бэкап + `node --check` ДО деплоя; не ломать живые флоу.
- Никаких диагнозов/дозировок «вслепую» в ответах; медицинское — «рекомендовать врача».
- Тон «забота», без личных имён (только «нутрициолог»/«специалист»).
- Игорь тестит вживую (телефон=клиент @viael_care_bot, Mac=нутрициолог в группе) и шлёт скрины.

---

## Сделано 2026-06-03 (всё в проде + `main`)

**A. Честность вывода ИП** (pro/pro-expert/elite, синтаксис-чек inline-JS, пуш):
- QoL учитывает мужские/системные жалобы (витальность, либидо/эрекция/мышцы/
  усталость, ЖКТ/урология/GSM/суставы) — больше не застывает на 100 у мужчин.
- Sanity биоимпеданса: `bioClamp()` в HTML обнуляет нереальные значения в источнике
  + гейт-бэкстоп в воркере (жир 3–60, мышцы 20–70, висцер 1–40, ИМТ 10–70, биовозр
  15–120). Причина: number-инпуты пропускают любое введённое с клавиатуры.
- Чекбоксы: делегированный обработчик `.chips` — «ничего/не тренируюсь/стабильно»
  взаимоисключающи + пары улучшилось↔ухудшилось.
- Карточка биозагрузки: число/кольцо = нагрузка (не готовность); бар сна вмешивает
  глубокий сон/пробуждения. Порог ЧД выровнен 12–20 (воркер ↔ карточка).
- `setBioHints()`: числовые подсказки-диапазоны в плейсхолдерах биоимпеданса.

**B. VIO** — Pro-апселл («Розблокувати Pro») переведён на все 12 языков (был UA-хардкод).

**C. КАНАЛ ВЕДЕНИЯ — построен с нуля, в проде, протестирован end-to-end** (см. §17):
- Клиентский бот **@viael_care_bot** (отдельный от backstage), webhook `/care-webhook`.
- Topics-супергруппа «VIA-L Ведение» `NUTRITIONIST_GROUP_ID=-1003907260827` (бот-админ),
  топик-на-клиента, двусторонний автоперевод (Haiku / Sonnet для he/ja/ko, uk/ru без).
- Deep-link `?start=<lang>` (язык из ИП, «прилипает»); кнопка ELITE `#telegramBlock` → бот.
- **`/anketa`** в топике → клиенту локализованное приглашение + ссылка на полную анкету
  `https://via-l.com/book/anketa/via-l-anketa-zdorovya.pdf`.
- Секреты `CLIENT_BOT_TOKEN`, `NUTRITIONIST_GROUP_ID`. Словарь `CARE_PHRASES` (0 API).
- Из ивритских строк ELITE убраны личные имена «Marina».

---

## Задачи на следующую сессию (по приоритету)

1. **Цифровизация полной анкеты** (главная цель) — 110-вопросный PDF (`book/Анкета/`,
   блоки A–M, анализы в B2/G/H/I/J/K) → веб-форма × 12 языков + ИИ-структурирование
   для нутрициолога (как Expert-профиль). Сейчас доставляется как PDF (костыль).

   **✅ СДЕЛАНО 2026-06-04 — В ПРОДЕ + `main`. Полная механика → `ARCHITECTURE.md` §18.**
   Кратко: форма `book/anketa/index.html` (110 Q, 12 яз) → `/anketa-submit` → ИИ-сводка
   (Sonnet 4.6, RU, как Expert-профиль) + полные ответы нутрициологу в TG-топик + email
   клиенту. Команда `/anketa` шлёт веб-форму вместо PDF. Мгновенный ответ формы +
   фоновая доставка (`ctx.waitUntil`). Авто-пересоздание топика при удалении
   (`careRelayToTopic`). Обкатано end-to-end вживую (Игорь как тест-клиент).

   **Остаток по анкете (перенесён в общие задачи ниже):** локализация email-письма на
   12 языков; авто-выдача ссылки на анкету при переходе в ведение (сейчас вручную через
   `/anketa`); опц. перевод полных ответов на RU; доперевод формы на 8 языков (EN-fallback).
2. ✅ **FAQ-память «эталон»** — СДЕЛАНО 2026-06-04, в проде (см. ARCHITECTURE §16).
   База `faq:db` (KV, пары {q,a,ts} на ru, ≤500). Кнопка «⭐ Сохранить в эталон» под
   «✅ Ответ доставлен» в топике → пополняет базу ТОЛЬКО по подтверждению человека.
   Поиск похожего — СЕМАНТИЧЕСКИЙ (1 вызов Haiku, ловит синонимы/перефраз), подсказка
   `💡 Похожий эталонный ответ` уходит нутрициологу в топик (клиенту авто НЕ шлём).
   Webhook care-бота перерегистрирован с `callback_query`. Обкатано на живом.
   **Осталось:** засев базы 400–500 выверенных Q→A (черновик Claude → вычитка Maryna →
   разовый сидер в `faq:db`); начать с ~50 частых.
3. ✅ **Словарь фраз** — СДЕЛАНО 2026-06-04, в проде. `CARE_PHRASES` расширен с 3 до 7
   подключённых ключей × 12 языков: `got_attachment` (фото/файл/голосовое клиента →
   `copyMessage` в топик), `zoom_invite` (команда нутрициолога `/zoom <ссылка>`),
   `error_unsupported` (стикер/без текста), `error_generic` (релей не прошёл). Исправлен
   баг: медиа без текста раньше уходило в ветку `/start` и терялось. `bot-phrases.md`
   отмечает реализованные ключи; остальные фразы — спека/TODO (заводить под реальный
   триггер). Команды нутрициолога: `/anketa`, `/zoom <ссылка>`.
4. ✅ **Cal.com / анкета при брони** — СДЕЛАНО 2026-06-04, в проде (см. ARCHITECTURE §18.10).
   Free-план Cal не даёт править workflow-письма → шлём сами из `/cal-webhook`: при брони
   60-мин (≥45 мин) ELITE/несвязанным уходит EN-led письмо со ссылкой на анкету + нота про
   12 языков; программным (метка `prog_email`) НЕ шлём. **Плюс крупное:** программы (4 шт)
   переведены с маленькой `program-intake` на большую `book/anketa` (ссылка в письме об
   оплате `?intake=<token>` → сводка+кнопка «Назначить сессию» в backstage). Форма шлёт
   сабмит при `topic`/`intake`/`src`. Обкатано вживую (Игорь). program-intake.html дремлет.
5. **Хвосты анкеты** (база готова, §18): ✅ (а) email `ANKETA_MAIL` локализован на 12
   языков (2026-06-04); ✅ (б) авто-выдача анкеты при переходе в ведение — в ветке
   `/start` care-бота при ПЕРВОМ подключении создаётся+привязывается топик, нутрициолог
   получает «🆕 клиент подключился», клиенту автоматически уходит `anketa_invite` с
   `&topic=<id>`; идемпотентно, ручной `/anketa` сохранён (2026-06-04). **⚠️ Только в
   коде — НЕ задеплоено: воркер нужно `cd interpreter && wrangler deploy` + ретест на
   живом.** Остаток: (в) опц. перевод «📋 ПОЛНЫЕ ОТВЕТЫ» на RU; (г) доперевод формы на
   de/pt/fr/pl/it/he/ja/ko.

**Хвосты:** PRO+EXPERT анкета — фронт есть, добить Apps Script под `product_id 7838826`
(`project_pro_expert_anketa_todo`); Oura production OAuth (ждёт кредов, §16/§15);
дообогащение нутри-кластера.

---

## Интеграции носимых устройств — статус и ОЖИДАНИЕ (параллельная ветка работ)

Каркас единый (Worker `/<vendor>/start|callback|metrics`, KV `WEARABLE_TOKENS`, фронт-функции `connectWearable`/`fetchWearableLive` общие). Детали — `ARCHITECTURE.md` §15–16, статус — memory `project-wearable-integrations-status`.

- **✅ Готово в коде, ждёт настройки владельцем** (register app → `wrangler secret put` → `wrangler deploy`):
  - **Fitbit** через Google Health API — секреты `GHEALTH_CLIENT_ID/SECRET`. OAuth проверен, live-данных нет (у владельца нет Fitbit).
  - **WHOOP** — developer.whoop.com, секреты `WHOOP_CLIENT_ID/SECRET`.
- **⏳ ПИСЬМА/ЗАЯВКИ ОТПРАВЛЕНЫ — ЖДЁМ ОТВЕТА:**
  - **Oura** — заявка на партнёрство 2026-06-02 (форма `ouraring.com/business`; первый контакт `api@ouraring.com`, **ticket #6994484**). Ответ ждём ~до середины июня (до 10 раб. дней). Прямого API нет — только партнёрский трек. Тишина → писать по тикету.
  - **Garmin** — заявка 2026-06-02 («Contact Garmin Health», `garmin.com/.../wellnesspartner/`), нужны Health API + Women's Health API. Коммерция платная, есть бесплатная eval-фаза. Ждём «Garmin associate».
  - Придут `client_id/secret` → собрать роуты + карточку по каркасу (§15).
- **📨 Заявку ещё НЕ слали (ОТДЕЛЬНАЯ, «вторая» задача):**
  - **Ultrahuman** — партнёрский доступ (не self-serve). Составить и отправить заявку на партнёрство (`partnerships.ultrahuman.com`) в ключе Oura/Garmin; затем интеграция (OAuth `partner.ultrahuman.com`, scopes `ring_data cgm_data profile`) + добавить в галерею/карточки.
- **🔜 Следующие self-serve по очереди:** **Polar** (AccessLink) → **Withings**. Фронт уже общий — нужны только worker-роуты + карточка + i18n. Игорь просил по одному; следующий — **Polar**.
- **❌ Без веб-API (файл/ручной ввод):** Apple, Samsung, Xiaomi, Amazfit.

---

## Правила и операции (важно)

- Одно конкретное предложение прозой, без опросников. Короткие ответы (3–8 строк).
- Approval per-file — не переносить паттерн с тарифа на тариф без «ок».
- **«Аккуратненько»:** бэкап + синтаксис-чек inline-JS (`node vm.Script`) ДО коммита.
  Паттерн: правка pro → чек → ретест Игорем на живом → перенос в pro-expert/elite.
- **Деплой:** HTML → GitHub Pages (`git push`, домен **via-l.com**); воркер →
  `cd interpreter && wrangler deploy` (OAuth viaelcom@gmail.com). Секреты —
  `wrangler secret put` (НЕ в код, репо публичный).
- **Два бота, один воркер:** `/tg-webhook` backstage (`TELEGRAM_BOT_TOKEN`),
  `/care-webhook` клиентский (`CLIENT_BOT_TOKEN`). Care-код — конец `cloudflare-worker.js`.
- Папки `Infa Cloude/N-*` — провенанс (gitignore), не коммитить.
