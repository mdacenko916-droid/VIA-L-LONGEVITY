# Oura (кольцо)

**Краткий статус (ПЕРЕСМОТРЕНО 2026-06-12 по офиц. докам Oura):** партнёрство для кодов НЕ нужно —
прошлый вывод «регистрация закрыта» был ошибочным. По офиц. докам (cloud.ouraring.com/docs,
support.ouraring.com): регистрация OAuth-приложения — **самообслуживание** на
`cloud.ouraring.com/oauth/applications` (вход под аккаунтом Oura → имя+сайт → сразу Client ID +
Client Secret). **По умолчанию лимит 10 пользователей** — готовые коды сразу работают для до 10
клиентов. Одобрение (submit for review) нужно ТОЛЬКО для выхода за 10 («wider audience») — это и
есть «партнёрская» дверь, которую Becky закрыла 🔴 2026-06-12 (тикет #6994484: «новых партнёров не
берём, свяжемся сами»). **ВЫВОД: отказ блокирует только масштаб >10 Oura-клиентов, НЕ старт.** Для
первых ≤10 клиентов прямой Oura (Readiness/Sleep Score, точные цифры) включается без чьего-либо
одобрения. **Реальный блокер = рабочий аккаунт/кольцо Oura у владельца** (покупка затерялась → не
под чем войти на портал; именно поэтому «самрегистрация не работала» — не была закрыта, не было
аккаунта). PAT Oura убрал дек 2025 → только OAuth2. Параллельно рабочий путь данных без кодов
остаётся **Apple Health** (кольцо → Oura app → Health → мост `healthkit-bridge.js`, ночное среднее
SDNN) — он же покрывает клиентов без прямого подключения.

## Сейчас (как есть)
- В тарифе **VIO** работает демо-режим: кнопка «✨ Загрузить демо-данные» подставляет
  примерные цифры (не настоящие данные клиента). Это нормально для бесплатного тарифа.
- ✅ **2026-06-11: мёртвая токен-вкладка убрана из PRO / PRO+EXPERT / ELITE.** Oura удалил
  Personal Access Tokens в дек 2025, а карточка звала «создать токен» → вводила в заблуждение
  (жена владельца тыкала, не работало). Заменено честной заметкой `instr_oura_connect` ×12:
  «в приложении кольцо читается через Apple Health; прямое онлайн-подключение — скоро» + остался
  файл-экспорт. Рабочий путь Oura сейчас = **Apple Health** (кольцо→Oura app→Health→наш мост).
  **VIO не трогали** (по решению владельца — там демо-режим Oura Sandbox как был).
  Дед-JS `setMethod('oura')`/`fetchOura` остался, но не вызывается (кнопок нет) — безвреден.

## Сделано
- Демо для VIO.
- Старый путь с ручным токеном (теперь мёртв — Oura его отключил).
- ✅ **2026-06-12: фикс возврата в нативном app после OAuth.** В app вход Oura открывается в
  системном браузере (Safari) и обратно НЕ редиректит → приложение не узнавало об успехе. Решение
  БЕЗ нативного кода: `connectOura` ставит `vial_oura_pending`, а `visibilitychange→visible` (≤10 мин)
  до-тягивает метрики по `sid` (токен уже в KV воркера). Работает и в браузере (там ещё и редирект-IIFE).
  «Чистое» решение (custom URL scheme deep link) НЕ делали — требует rebuild + Info.plist (а `app/ios`
  в .gitignore, не персистится), текущее достаточно. Коммит `72e7b26`.

## В процессе / следующий шаг
- ✅ **2026-06-12: приложение Oura зарегистрировано (self-serve), интеграция ПОСТРОЕНА и задеплоена.**
  Client ID `65bcca60-…` заложен (`wrangler secret put OURA_CLIENT_ID`). В воркере — роуты
  `/oura/start` `/oura/callback` `/oura/metrics` (по образцу WHOOP; OAuth2, refresh, KV `oura:<sid>`,
  scopes `personal daily heartrate workout spo2`). Метрики: HRV=`average_hrv` (ночной RMSSD!),
  RHR=`lowest_heart_rate`, сон/deep, SpO2, readiness→energy, + **тренировки** (`/v2/usercollection/workout`).
  Фронт: кнопка «🔗 Подключить кольцо Oura» в карточке Oura всех трёх тарифов (self-contained
  `_ouraUI`/`connectOura`/`fetchOuraLive`, 12 языков, синхрон в setLang).
- 🔑 **ОСТАЛОСЬ от владельца: заложить Client Secret** — `cd interpreter && wrangler secret put OURA_CLIENT_SECRET`
  (вставить секрет по запросу). Без него `/oura/start` отвечает `oura_secrets_missing`.
- ⏳ **Живой тест:** после секрета — нажать «Подключить кольцо», пройти OAuth, проверить что метрики
  пришли. ⚠️ Имена полей Oura v2 сверить на первом ответе (парсинг защищён — null не падает, но
  если поле названо иначе, метрика будет пустой → поправить в `handleOuraMetrics`).
- ✅ **2026-06-12: интеграция ПОДТВЕРЖДЕНА вживую** (скрин владельца): HRV 22 мс (=average_hrv,
  ночной RMSSD!), пульс 61, сон 5.7 ч, deep 70 мин, готовность 9/10 — все поля Oura v2 замаппились
  верно. ✅ Тренировки заведены в AI-разбор: `data.training` (фронт, из `importedData`) →
  `buildUserMessage` строка о нагрузке за неделю + HRV-направленная нагрузка (воркер `eae6c57d`).
  Активируется только при наличии workout-данных (graceful). ⏳ Проверить на клиенте с залогированной
  тренировкой, что строка реально появляется (у владельца в скрине тренировок не было видно).

## В ожидании (пассивно — НЕ инициировать)
- 🔴 **Партнёрский трек / одобрение для >10 пользователей — ОТКЛОНЁН 2026-06-12** (тикет #6994484,
  Becky, Oura Partners): «новых партнёров не берём, свяжемся сами». Это нужно ТОЛЬКО для масштаба
  сверх 10 Oura-клиентов. **Больше не писать по этому треку.** Вернёмся к review-заявке, когда
  реально упрёмся в лимит 10 (= появятся >10 платящих Oura-клиентов) — тогда у заявки будет вес.

## Что нужно сделать
1. ✅ Самообслуживающая регистрация приложения Oura — СДЕЛАНО (партнёрство не требуется до 10 польз.).
2. ✅ Client ID заложен; роуты `/oura/*` + кнопка подключения ×3 тарифа — СДЕЛАНО и задеплоено.
3. 🔑 **Владелец:** `wrangler secret put OURA_CLIENT_SECRET` (единственное оставшееся).
4. ⏳ Живой OAuth-тест на кольце + сверка имён полей Oura v2 в `handleOuraMetrics`.
5. ⏭️ Завести тренировки в AI-промпт (`buildUserMessage`).

## Зачем прямой Oura, если кольцо уже читается через Apple Health (причина и необходимость)
Apple Health — это **широкий канал-приёмник** (Watch, кольцо через Oura app, и т.д.), он нужен и
остаётся. Но для Oura-метрик он структурно неточен по двум причинам, и обе НЕ чинятся на стороне
Apple:
1. **Разные метрики HRV.** Apple Health хранит только **SDNN** (`HKQuantityTypeIdentifierHeart`
   `RateVariabilitySDNN`), а в приложении Oura пользователь видит **RMSSD** (ночной). Это разные
   величины из одного RR-ряда — совпадения цифр не будет в принципе, только сближение порядка.
2. **Разная агрегация.** Apple пишет SDNN множеством разовых замеров за сутки (в т.ч. дневных от
   Watch), Oura считает среднее за период сна. Наш мост `healthkit-bridge.js` теперь усредняет
   HRV **в окне сна** (а не «последний сэмпл за 7 дней», как было до 2026-06-12) — это сильно
   ближе к ночной HRV кольца, но всё ещё SDNN, не RMSSD.
- Плюс через Apple Health **недоступны** фирменные сводные индексы Oura: **Readiness Score**,
  **Sleep Score**, температурные тренды, фазы сна по версии Oura. Их отдаёт только Oura API.
**Вывод:** прямой Oura нужен там, где важна (а) сопоставимость цифр с тем, что человек видит в
приложении Oura, и (б) фирменные скоры Oura (Readiness/Sleep). Apple Health это дать не может —
ни сейчас, ни после любой доработки моста. Поэтому интеграция остаётся в нуждах, а не «вместо нас
всё закрывает Apple Health».

## Важно знать
- ⚠️ **Самостоятельная регистрация приложения НЕ работает** — доступ закрыт, нужна покупка
  (проверено владельцем многократно). Не предлагать снова «зарегайся на cloud.ouraring.com».
- Personal Access Tokens Oura убрал в декабре 2025 → старый токен-путь в PRO/ELITE мёртв,
  инструкции на 12 языках надо снять; OAuth теперь единственный путь.
- Данные приходят **от каждого клиента** (его кольцо + его активная подписка Oura).
- Клиенты Gen3 / Ring 4 без активной подписки Oura данные через API не отдают.
