# Fitbit

**Краткий статус:** готов и задеплоен; устройство у владельца (приехало 2026-06-12,
раньше срока). Живая отладка 2026-06-12: починены client secret и фильтр дат,
ждём первую ночь с гаджетом для проверки данных.

## Отладка 2026-06-12 (живое устройство)
- ✅ «Ошибка 403 access_denied» при входе — OAuth-приложение в Google Cloud в режиме
  Testing; владелец добавлен/входит как тестовый пользователь (проект `via-l-health`,
  client `697386102285-…oft`). Для реальных клиентов нужен перевод в Production +
  верификация Google (health-скоупы, недели) — следующий блокер перед запуском.
- ✅ `invalid_client` на обмене кода → секрет был протухший; новый заведён
  `wrangler secret put GHEALTH_CLIENT_SECRET` (2026-06-12).
- ✅ Фильтр daily-типов: Google принимает только `>=` и `<` (не `<=`) —
  исправлено в `handleFitbitMetrics` (верхняя граница = завтра, строго меньше).
- ⏳ Данные: API отвечает чисто, но точек 0 (гаджет носится первый день; один раз
  пришло 5 точек SpO₂ — поток идёт с задержкой). Проверка — после первой ночи.
- ⚠ Во воркере временные `FITBIT-DEBUG` console.log — убрать после проверки.
- ⚠ Карточка обещает «Шаги · Энергия», но scope `activity_and_fitness_readonly`
  не запрашивается и шаги не тянутся — решить, добавлять ли.
- ✅ **2026-06-12 (эта сессия): OAuth-возврат в app починен** тем же приёмом, что для Oura.
  `connectFitbit` ставит `vial_fitbit_pending`, при возврате в приложение `visibilitychange→visible`
  (≤10 мин) до-тягивает по `sid` (токен уже в KV). Очищается в `fetchFitbitLive` при успехе. Работает
  и в браузере (там прежний редирект-IIFE). Применено на pro/pro-expert/elite.

## Сейчас (как есть)
- Кнопка «🔗 Подключить» есть во всех трёх платных тарифах (PRO, PRO-EXPERT, ELITE).
- Важная особенность: Fitbit закрыл регистрацию новых приложений в своём старом API,
  поэтому данные Fitbit мы берём **через Google Health API**. Для клиента кнопка всё
  равно называется «Fitbit».

## Сделано
- Серверные адреса в воркере: `/fitbit/start`, `/fitbit/callback`, `/fitbit/metrics`.
- Кнопка и тексты во всех платных тарифах, на 12 языках.
- Разбор данных: HRV, пульс покоя, SpO₂, сон (общий + глубокий).

## В процессе
- Ничего активного.

## В ожидании
- Нет ожидания вендора — путь самостоятельный.

## Что нужно сделать
- Настройка в Google Cloud Console сделана, секреты `GHEALTH_CLIENT_ID/SECRET` заведены,
  задеплоено (2026-06-04). Дополнительных действий по запуску нет.
- Осталось одно: **проверить на живом устройстве** — у владельца нет Fitbit, поэтому
  разбор данных ещё не проверяли на реальной истории (сам вход/разрешение проверены).

## Важно знать
- Температуру тела (ночное отклонение) Google не отдаёт в нужном виде — этот один
  показатель сознательно пропускаем, остальное берём.
