# TODO — единый список открытых задач

> **Единственный живой список задач проекта.** Закрытую задачу — УДАЛЯЙ отсюда,
> а факт выполнения коротко записывай в архитектуру (root `ARCHITECTURE.md` §9
> «Журнал» + детальная механика в `interpreter/ARCHITECTURE.md` соответствующего §).
> Так этот файл всегда = только то, что ещё не сделано.
>
> **Обновлено: 2026-07-16.** Активный продукт App Store — **только VIA-L** (`interpreter-via-l.html`);
> VIO/PRO не ведём (см. «🗄 АРХИВ VIO/PRO»). Контекст по любому пункту — `ARCHITECTURE.md` (root) и
> `interpreter/ARCHITECTURE.md` (§15–18), плюс memory.

---

## ★ Связь клиент↔специалист + VIA-L EXPERT (зафиксировано 2026-07-26, после согласования модели)

Контекст: memory [[project_own_chat_reply_gap]], [[project_tariff_naming_canon]],
[[project_platform_specialists]]; `docs/PLATFORM-MODEL.md`.

**✅ Сделано и в проде 2026-07-28 (запуск VIA-L EXPERT):** цепочка оплаты
проверена вживую (купон 100%) — оплата Hotmart (product_id 8199953/8200155) →
webhook → выдача кода `VIAL-EXPERT-8W/12W` → письмо клиенту с кодом+ссылкой →
вход по коду в приложение → карточка в кабинете. Заодно: ренейм тега
ELITE→VIAL-EXPERT во всей цепочке (воркер+apps-script+code-generator+гейт);
чистка видимых имён (approach_body/бейджи «VIA·L PRO»→VIA-L/VIA-L EXPERT);
письмо ведёт на приложение EXPERT (ipLinkFor) и без дубля бренда; **вернули
платный гейт кода на вебе EXPERT** (был осиротевший) + блокировку по сроку
(56/84 дня, expiry). Операционка (webhook URL, HOTMART_TOKEN, apps-script
redeploy) — в [[project_tariff_naming_canon]].

- [ ] **Свой чат — ответ специалиста из кабинета.** Клиентская сторона Expert-чата
      готова и мультиспециалистна (код доступа → карточка `clients` → `specialist_id`,
      `/expert/thread` тянет язык спеца и переводит `out`). Разрыв: ответ спеца (`out`)
      попадает в `d.messages` только из Telegram-флоу утверждения PDF (воркер ~3278);
      `/cabinet/tg-send` (~5843) шлёт только в Telegram-топик и НЕ пишет `out`. Нужен
      `/cabinet/chat-send`: пишет `{dir:'out',source:'cabinet',date,text}` в карточку
      (автоперевод на язык клиента уже есть в tg-send), опц. дублирует в топик →
      кабинетный ответ виден в чате клиента, Telegram → опциональный мост.
- [ ] **VIA-L EXPERT — нативная обёртка.** Сейчас `interpreter-via-l-expert.html` —
      только веб (открывается в браузере). Завернуть в Capacitor (как `app/`) для
      простой установки на телефон, как у VIA-L. Отдельная задача.
- [ ] **Экран «Мой наставник» без кода = тупик (VIA-L).** Холодный покупатель без
      реф-ссылки упирается в голое поле кода. Сделать трёхслойным: 1) пришёл по `?ref`
      → код подставлен, кнопка «Подключиться»; 2) код есть → ручной ввод; 3) кода нет
      → «Подобрать наставника» (витрина/лёгкий подбор, все языки, не ru/uk-«знакомство»).
      См. PLATFORM-MODEL §10 задача №3.
- [ ] **Класс B — внутренние id (опц., не срочно).** `MAX`/`tier:'pro'`/лоу-кейс
      `'elite'`/`'pro'` (флаги режима ИИ в /analyze) + ~40 ELITE-комментариев. НЕ ломает
      ничего; чистить только пачкой при желании нулевых вхождений. См. [[project_tariff_naming_canon]].

---

## ★ Индекс дня — хвосты после доказательной пересборки (2026-07-20)

Контекст: [[project-index-scoring-evidence]], [[project-horm-symptoms-field]],
`docs/INDEX-SCORING-SOURCES.md`, `docs/PERSONAL-BASELINE-RECON.md`. Выгрузки
OpenEvidence в `interpreter/Infa Cloude/N-6/` (владелец тянет по запросу).

**✅ Сделано и в проде 2026-07-20:** индекс на доказательной базе (веса по
AMS/MRS, возрастные пороги HRV убраны из индекса+плитки+промпта воркера);
личная норма HRV (сглаживание 3 дня, база ≥14) + частотный коридор приливов;
тексты без называния гормонов/дефицитов (приложение + guardrail воркера);
мужское И женское гормональное начисление оживлено (баг:
читали мёртвое поле `horm_symptoms`); плитки (личная норма, «i»-объяснения,
«X» в шторке); дневной блок «Что нового за сутки»; «ИИ»→«VIA-L».

**✅ Сделано и в проде 2026-07-20 (вторая сессия):**
- **SpO₂ починен по-настоящему** (воркер `51c1249b`, живой лог Fitbit): запрос
  сужен до 44 ч (было 8 дн → упор в потолок 1000 сэмплов, обрезка); усреднение
  по **окну сна** последней ночи, а не по календарному дню (бакет «по дню»
  разрывал ночь 22:03→06:23 и подмешивал утро); SpO₂ = среднее по **качественным
  замерам ≥90%** (Google Health отдаёт СЫРОЙ поток, 58% сэмплов <90% из-за
  артефактов → mean 88 vs телефон 96; ≥90% → ~95, сходится), при реальной
  десатурации (<20% ≥90) → честная медиана. Подтверждено вживую: 88 → 95.
- **Частота дыхания** → личная база (+3 вд/мин над медианой 3 дней, minBase 7;
  абсолютный порог 20 как запасной путь без базы). HTML.
- **Перелёт** — мёртвое правило удалено (читало `data.events`, которого нет ни
  в `data`, ни в анкете → не срабатывало; перелёт идёт в ИИ через `lifestyle_notes`).
- **Блок «Тренды за 30 дней»**: сворачивается (шеврон в шапке, по умолч. свёрнут →
  сводка «N улучшилось / без изменений / ухудшилось» + названия); спарклайны в
  цвет иконки метрики (`_TR_COL`); опущен под «Памятку дня» (ретроспектива в конце).

**⏳ Осталось:**
- [x] **Личная норма для сна/глубокого/энергии** (сделано 2026-07-20, HTML):
      без литературного CV — разброс из СВОЕЙ истории (`_pNorm`, MAD; «ниже
      обычного» = свежее ниже базы > max(1.5×MAD, 7%)). ТЕКСТ-наблюдения, БЕЗ
      балла (не задваивать с абсолютными правилами). tempDev пропущен (сам
      «отклонение»), sleep_qual пропущен (уже сильная абсолютная карточка).
- [ ] **Живая проверка** текста разбора: без «норма для возраста»/«нехватка
      магния»; заметка «простужен» учитывается. (SpO₂-часть закрыта.)
- [x] **Кэш импорта → свежесть каждого прохода** (Fitbit, сделано): при загрузке
      тихо до-тягиваем метрики по сохранённому sid без OAuth/popup
      (`_refreshFitbitSilent`, токен в KV). Проход не сидит на `vialp_imported`.
      ⏳ то же для Oura/Polar/Withings по образцу — когда понадобится.
- [x] ~~прибрать мёртвый s_ns_desc~~ — НЕ делаем (2026-07-20): ключ живой
      (`data-t="s_ns_desc"`), «мёртвое» = дубли в _NSK/_NSK2/_NSK3, перекрытые
      поздним дневным блоком. На UI/скорость не влияет; вырезать вложенный
      {×12} из больших i18n-словарей — риск регресса ради нулевой пользы.
- [ ] **Специалисту** отдать `docs/INDEX-SCORING-REVIEW.md` (3 вопроса: старт
      70, «второе дыхание», «низкая температура+усталость») + таблицу весов §8.
      Ссылки проверены веб-поиском, выдуманных нет. MRS реализовывать нельзя
      (лицензия ZEG Berlin), ссылаться — можно; AMS свободна (SOURCES §1.1).

---

## ★ Фичи-двойники из OpenEvidence N-23/10:1 (заведено 2026-07-24)

Контекст: доп-ответ OpenEvidence по двойникам «низкого прогестерона» — апноэ сна (ОАС) и
ПМДР vs депрессия. Клинический текст уже впитан в KB (ОАС-фенотип у женщин → P-F17;
временной рисунок настроения → P-F3 + **P-F9 усилен 2026-07-24**). Ниже — не-KB, продуктовые
идеи, которые упираются в каденс сбора и новые поля (поэтому в TODO, а не в KB). Сырьё —
`interpreter/Infa Cloude/N-23/10:1.txt` (gitignore). Связка: [[project_ip_weekly_report]],
[[project_pro_funnel_structure]], [[project_kb_pattern_audit]].

- [ ] **Цикл-карта симптомов настроения (аналог DRSP).** Мы уже собираем и фазу цикла, и
      настроение/тревогу — можем строить карту симптомов по циклу и дескриптивно подсвечивать
      «симптомы весь цикл, а не только перед месячными → обсудите с врачом» (различение
      ПМДР / предменструальное обострение / первичное расстройство). ⚠️ **Стопор:** сейчас
      настроение/тревога/стресс идут НЕДЕЛЬНО/двухнедельно (канон ритма), а для настоящей
      DRSP нужна ДНЕВНАЯ привязка к фазе ≥2 циклов. Значит: либо повысить каденс сбора
      настроения (мин. дневная отметка с тегом фазы), либо ограничиться грубым недельным
      паттерном. Требует решения по каденсу + UI карты. Только дескриптивно, без диагноза.
- [ ] **ОАС-скрин-модуль (осведомлённость + мягкий флаг).** ОАС у женщин среднего возраста
      сильно недодиагностирован, растёт в менопаузе, лечится; фенотип — бессонница/усталость/
      настроение, не храп. Модуль: риск-факторы, что клиент опознаёт сам (храп, ноктурия,
      утренние головные боли, гипертония, ИМТ>30, ранняя/хирургическая менопауза) + носимые-
      сигналы (высокий ночной пульс, частые пробуждения, низкий SpO₂) → «обсудите со
      специалистом про исследование сна», НЕ диагноз. ⚠️ Новых входных полей (храп/ноктурия/
      апноэ) сейчас нет — нужно добавить в опросник. STOP-опросник недооценивает женщин
      (пол-пункт=0) → порог ниже.

Контекст/детали — memory [[project-vial-file-renamed-via-l]], [[feedback-appstore-hotmart-disconnected-iap-only]], [[project-vial-only-appstore-vio-pro-dropped]].

**✅ Сделано в коде (сессия 2026-07-16):**
- IAP-paywall (#iap-gate) под Guideline 3.1.2: добавлено раскрытие авто-продления (`iap_renew_note`) +
  ссылки Условия/Политика прямо на экране подписки, 12 языков.
- `Info.plist`: HealthKit purpose-строка → EN (dev region=en); убран `NSHealthUpdateUsageDescription`
  (приложение только читает, `write:[]`); добавлен `ITSAppUsesNonExemptEncryption=false`.
- **Починен `app/sync-web.sh`**: точка входа = `interpreter-via-l.html` (был редирект-заглушка pro) +
  копирует `phosphor/`,`icons/`,`food/`; `app/www` пересобран.
- Перевод выровнен: 703 ключа × 12 языков, пробелов/утечек кириллицы нет; `<html lang>`→en.

**⏳ Осталось — сборка/стор (владелец):**
- [ ] `cd app && npx cap sync` → сборка в Xcode, залить билд.
      ⚠️ известный блокер окружения: `pod install` падает (Ruby 4.0.5 + CocoaPods 1.16.2,
      `Encoding::CompatibilityError`). Обход: `cd app/ios/App && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 pod install`
      (нужен только при смене нативных плагинов; веб-контент копируется и без него).
- [ ] App Store Connect: auto-renewable подписка €15/мес → привязать к RevenueCat
      (entitlement `via_l_pro`, bundle `com.viael.interpreter`).
- [ ] App Privacy nutrition labels: задекларировать HealthKit-данные (не для рекламы/трекинга).
- [ ] Метаданные/скриншоты без мед-обещаний + демо-доступ ревьюеру (обход IAP-гейта, sandbox-аккаунт).
- [ ] Подтвердить корректность `ITSAppUsesNonExemptEncryption=false` (только стандартное шифрование).

**⏳ Осталось — продукт VIA-L (перенесено из старого VIO-блока, актуально):**
- [ ] **Legal:** вычитка носителями 11 языков + хостинг `legal-app/` на чистом лендинге → URL в App Store Connect.
- [ ] **Долговременное хранение данных клиента — Уровень 1–2.** Сейчас всё в localStorage WebView (хрупко).
      ✅ Есть: бэкап export/import (Ур.3), cap метрик `*_daily` 1500 (≈4 г) / `*_ai_daily` 730 (≈2 г).
      🔴 Открыто: Ур.1 (нативный стор через Capacitor) + Ур.2 (iCloud/Google Drive + legal-согласие). [[project-storage-durability]]
- [ ] **Прогон на реальном устройстве:** карточка, авто-язык на iPhone, кэши день→день, RTL иврит,
      Capacitor-сборка, шифрование round-trip, советник (ответы по данным клиента + велнес-гардрейл).
- [ ] опц.: глубокий фолбэк «Памятки дня» на 12 языков (сейчас EN — App-Store-safe по решению владельца).

---

## ★ Главная ИП `interpreter/index.html` — VIO+PRO убраны (2026-07-16, ✅ основное)

**✅ Сделано:** удалены секция «выбор источника» (vp-vio/vp-pro), карточки тарифов Vio(€9.90)+Pro(€29),
все ссылки на `interpreter-vio.html` и мёртвые функции (ouraAuto/ouraManual/startV10/startPro/goToInterpreter);
заголовок/подзаголовок секции переписаны на 12 языков («Ведение с нутрициологом / Два уровня живого
сопровождения» — вместо вводящих в заблуждение «Три уровня / Начинай бесплатно»); Expert-карточка
отцентрирована (осталось 2: Expert + Elite full-width). Синтаксис/T-объект валидны, запушено.

**✅ Добито 2026-07-16 (вторая правка):**
- `interpreter-vio.html` **УДАЛЁН** из репо (архив с ассетами → iCloud `Для IPhone Ihor/Интерпретатор /VIO/`);
  ссылка «← Інтерпретатор» в `methodology.html` переведена на `./index.html`.
- Буллет «Все з тарифу Pro» убран из фич-листа Expert (12 языков + HTML-дефолт).
- Мёртвый CSS (`.vp-*`, `.plan-free*`, `.plan-pro*`, `.btn-free`, `.btn-pro`) и 28 мёртвых T-ключей
  (`vp_*`, `vp_how_*`, `plan_free_*`, `plan_pro_*` × 12 языков = 171 свойство) вычищены. −20 КБ.
  Проверено: 12 языков целы, живые ключи тарифов на месте, синтаксис OK.

**⏳ Осталось:**
- Прим.: в Elite оставлено «Все з тарифу **Pro + Expert**» — корректно, ссылается на карточку Expert
  (её название = «Pro + Expert»), которая на странице есть.

---

## 🔒 ИИ-воркер (общий `cloudflare-worker.js`) — заморозка compliance + анти-регресс

> Воркер обслуживает VIA-L (и EXPERT/ELITE). RC2 закрыт (тег `RC2`, воркер `5bdad750`). Здесь — только правила на будущее.

- **Заморожен compliance-фундамент** (не ослаблять без слова владельца): `temperature` (0.35),
  `AI_SAFETY_RULES`, `wellnessGuardrail`, `_stripHerbLines`. Менять по реальным дефектам, не вслепую.
- **АНТИ-РЕГРЕСС:** НИКОГДА не ослаблять детерминированный слой (`_stripHerbLines`/`wellnessHardScrub`/scrub)
  ради «более естественного» текста — приоритет всегда у compliance; каждое правило стоит на реальной утечке.
  Зелёный мех-скан ≠ «всё ок»: дезинформация без бан-слов проходит молча (урок «мелатонин→баланс сна») →
  периодический спот-чек живого вывода ГЛАЗАМИ, особенно EXPERT/ELITE.
- **Само-триаж дефектов:** дезинформация (ложь) = БЛОКЕР, чинить даже пост-фриз hotfix'ом; просто коряво (падежи) = полировка.
- Остаток: снять regen% из прод-логов (`grep '[GUARDRAIL]'`); guardrail на EXPERT/ELITE-черновик (advisor уже с claim-правилами).
- Каноны терминологии/claim'ов — [[feedback-wellness-terminology-mandatory]].

---

## 🗄 АРХИВ VIO/PRO — НЕ ведём (только генезис via-l)

> Ветка VIO/PRO закрыта ([[project-vial-only-appstore-vio-pro-dropped]]). Всё ниже — сделано-и-в-проде
> историческое, из чего вырос VIA-L; активных задач нет. Механика — в git history + `ARCHITECTURE.md` §9 +
> memory (раздел «🗄 АРХИВ VIO/PRO» в MEMORY.md). Оставлено одной строкой, чтобы не засорять живой список:
> перестройка тарифов под PRO (эталон interpreter-via-l.html), редизайны карточки/результата VIO,
> Oura-стиль ИИ + форк KB по тарифу, «Памятка дня», UI-доводки, паритет PRO=VIO, разделение хранилищ
> (VIO=vial_*, PRO→vialp_*=VIA-L). EXPERT/ELITE-канон (=тот же приём+анализ + слой специалиста) —
> в разделах ниже (кабинет/платформа/Hotmart) и в [[project-tariffs-aligned-2026-06-29]].

---

## 1. Носимые устройства — вторая волна (ждём доступ / настройку)

Каркас единый (Worker `/<vendor>/start|callback|metrics`, KV `WEARABLE_TOKENS`,
фронт `connectWearable`/`fetchWearableLive`). Детали — `interpreter/ARCHITECTURE.md` §15–16.

- **✅ В проде:** Fitbit (через Google Health API), Polar, Withings — секреты заведены.
- **⏳ Ждём ответа партнёра** (заявки отправлены 2026-06-02): **Oura** (ticket #6994484,
  `api@ouraring.com`), **Garmin** (Health + Women's Health API). Придут creds → собрать
  роуты + карточку по каркасу §15.
- **⛔ WHOOP — заблокирован членством.** Код code-complete и эндпоинты верны
  (`api.prod.whoop.com/oauth/oauth2/auth|token`, база `/developer/v2`, scopes
  `read:recovery read:sleep offline`). НО: создать app и получить `client_id`/`secret`
  можно ТОЛЬКО с аккаунта с активным WHOOP-membership + устройством — партнёрского
  канала/заявки у WHOOP нет (self-serve, гейт = членство). Тот же класс, что Fitbit.
  → Получить креды с аккаунта-члена (владелец оформляет membership ИЛИ реальный
  клиент-владелец WHOOP регистрирует app с redirect
  `https://interpreter.viaelcom.workers.dev/whoop/callback`) → `wrangler secret put`
  `WHOOP_CLIENT_ID`/`WHOOP_CLIENT_SECRET` → `deploy`. До этого — карточка есть, live ждёт.
- **📨 Ultrahuman** — реальный партнёрский процесс (НЕ self-serve). Составить и отправить
  заявку на `partnerships.ultrahuman.com`, затем интеграция (scopes `ring_data cgm_data profile`).
- **❌ Без веб-API** (только файл/ручной ввод): Apple, Samsung, Xiaomi, Amazfit.

---

## 2. Кабинет нутрициолога — полноценное рабочее место (редизайн) ← СЛЕДУЮЩИЙ БОЛЬШОЙ

**Модель согласована с владельцем (2026-06-05) → спека [`docs/CABINET-MODEL.md`](../docs/CABINET-MODEL.md).**
Цель: «1С, только лёгкое» — все данные клиента разбираются В КАБИНЕТЕ, а не в ТГ/ботах;
карточка = досье во времени (динамика по неделям), управление ботом из кабинета
(напоминания + ИИ-черновик ответа → утверждение специалистом).

Решения по спеке: хранилище — **Cloudflare D1**; вход — `/cabinet-auth` (убрать пароль из HTML);
в базу только оплаченные (сайт-ведение + ИП EXPERT/ELITE; НЕ VIO/PRO); карточка рождается при
оплате и привязана к коду доступа (тот же ключ, что у анкеты и ТГ-топика, §18.11).

**✅ Этап 1 (фундамент) — СДЕЛАН и в проде (2026-06-05):** D1 `vial-cabinet` + схема
`interpreter/cabinet-schema.sql`; `/cabinet-auth` (секрет `CABINET_PASS`, токен 12ч, rate-limit)
+ `/cabinet/clients|save|delete`; пароль убран из HTML, CRUD на D1, кнопка миграции из
localStorage. Воркер задеплоен, смоук-тест пройден. Детали — журнал ARCHITECTURE.md §9.

**✅ Этап 2 (ингест анкет) — СДЕЛАН и в проде (2026-06-05):** `deliverAnketa` (care/EXPERT) и
`deliverAnketaProgram` (site programs) upsert-ят клиента + answers + AI-сводку в D1 параллельно ТГ.
Вкладка «Анкета» в кабинете: язык/дата, сводка ИИ, все вопрос→ответ. Ошибки D1 не блокируют ТГ.

**✅ Этап 3 (биометрия + сессии + разборы) — СДЕЛАН и в проде (2026-06-05):** вкладки «Биометрия»
(недельная таблица, нед 1/4/8 выделены), «Сессии» (план+факт, авто-расписание от start_date),
«Разборы» (лог ИИ/PDF/Zoom с типом и статусом). Данные в D1 через saveData().

**✅ Этап 4 (переписка + ИИ-черновик) — СДЕЛАН и в проде (2026-06-05):** вкладка «Переписка» —
ручной лог in/out, кнопка «⚡ Черновик» (Claude Haiku по анкете+биометрике+сообщениям), редактор,
«Отправить в ТГ-топик» (/cabinet/tg-send → code_topic:<code> → careSend) + «Скопировать».

**✅ Этап 5 (поиск / статусы / мобайл) — СДЕЛАН и в проде (2026-06-05):** расширенный поиск
(имя+email+код+программа), фильтр по статусу (активен/новый/пауза/завершён), статус-бейджи в списке
и заголовке карточки + inline quick-change, пагинация (25 + «Показать ещё»), tabs overflow-x:auto.
Модал расширен: код, статус, программа, тариф, цена, формат, дата старта, длительность.

**Кабинет нутрициолога — ВСЕ ЭТАПЫ 1–5 завершены и в проде.** Открытые вопросы — спека §8.
Опциональные улучшения: Hotmart webhook → авто-создание карточки при оплате (§2); рассылки
по расписанию из кабинета (§5); полнотекстовый поиск по анкетам через D1 FTS.

---

## 2.5. Платформа для многих специалистов ← СЛЕДУЮЩИЙ БОЛЬШОЙ

**Спека на согласовании с владельцем (2026-06-06, уточнена) → [`docs/PLATFORM-MODEL.md`](../docs/PLATFORM-MODEL.md).**
ИП как инструмент для смежных специалистов (гинеколог/эндокринолог/андролог вокруг
пред-менопаузы/андропаузы): специалист рекомендует гаджет → клиент берёт **месячную подписку
PRO** → специалист ведёт его из кабинета (биометрия + ИИ-разборы). Живой эксперт = его врач;
**EXPERT/ELITE остаются воронкой основателя.** Монетизация: клиент платит подписку, специалист —
**абонентскую плату за доступ к платформе.** Принцип — готовое не ломаем: текущий нутрициолог =
«специалист №1».

Ключевое изменение правила: **PRO теперь заводит карточку**, если клиент пришёл по рефералу
специалиста (`ref_code`) → привязка `clients.specialist_id`. Сущности: `specialists`
(+`access_status`/`ref_code`/`lang`) + колонка `specialist_id` у клиента + мост перевода.

Этапы (спека §8): ✅ 1 фундамент → ✅ 2 подключение пациента (`/specialist/connect|unlink`) →
✅ 3 вход+видимость (логин PBKDF2, сессия {id,role}, 403) → ✅ 4 абонплата
(`/specialist-access-webhook` Hotmart + cron-истечение + email специалиста) → ✅ 5 мост перевода
(`/cabinet/translate`, ИИ-черновик на языке специалиста, авто-перевод при отправке, кнопки
«Перевести») → ✅ 6 панель владельца (`/cabinet/specialists|*`, вид «🩺 Специалисты»).

**ВСЕ ЭТАПЫ ПЛАТФОРМЫ 1–6 ЗАВЕРШЕНЫ И В ПРОДЕ.** Языки: 12 структурных (uk/ru/en/es/de/pt/fr/
pl/it/he/ja/ko); сам переводчик (Claude) не ограничен 12. he/ja/ko/ar → Sonnet.

### Локализация кабинета на 12 языков (→ продаваемый продукт для специалистов) ← В РАБОТЕ
Цель (решение владельца 2026-06-06): перед продажей подписки специалисту кабинет должен быть
полностью готов — интерфейс на языке специалиста (12 языков), без «допилим потом». Делаем
аккуратными партиями, каждая сразу на 12 языков; язык по умолчанию = язык специалиста
(`/cabinet-auth` отдаёт lang), владелец-ru не замечает миграции (фолбэк на ru).
- ✅ **Партия 1 (2026-06-06):** движок i18n (`T` key-major, `t()`, `applyI18n()` по data-i18n,
  переключатель языка), экран входа, верхняя навигация, 9 вкладок. `/cabinet-auth`→lang.
- ✅ **Партия 2 (2026-06-06):** окна клиента и специалиста (поля/опции/кнопки/заголовки),
  боковая панель (поиск/фильтр/сортировка), статусы клиента (`st_*` + `statusLabel()`).
- ✅ **Партия 3a (2026-06-06):** вкладка «Обзор», шапка карточки (мета/Старт/кнопки), панель
  «Специалисты» (таблица/бейджи/подсказки); helpers phaseLabel/specialtyLabel.
- ✅ **Партия 3b (2026-06-06):** Биометрия, Сессии, Разборы, Переписка (+статусы кнопок ИИ/отправки).
- ✅ **Партия 3c (2026-06-06):** Протокол, Анализы (19 показателей+группы+статусы), Заметки,
  Анкета, Календарь, список (всего/пусто/ещё), все confirm/alert + SPEC_ERR.
- ✅ **Партия 3d (2026-06-06):** PDF-отчёт (`generatePDF`) — на языке пациента (`tl`+PDF_LOCALE);
  сид `addDefaultProtocol` — на языке специалиста, дозы в нейтральных единицах (mg/g/IU/mcg).

**✅ ЛОКАЛИЗАЦИЯ КАБИНЕТА ЗАВЕРШЕНА — весь интерфейс + PDF на 12 языках, в проде.**
~275 ключей в `T`. См. [[project_cabinet_i18n]].

**✅ PWA (2026-06-06):** кабинет — устанавливаемое приложение (значок на экран, телефон/планшет/
десктоп). `cabinet/manifest.json` + `icon.svg` + `sw.js` (офлайн-оболочка, network-first HTML,
POST/cross-origin не трогает) + meta в head. Адрес для установки: **via-l.com/cabinet/**.
⏳ iOS-значок пока из SVG (упрощённый) — PNG-набор добавить при наличии конвертера/дизайна.

**✅ Вид под профиль (2026-06-06):** `/cabinet-auth` и `/cabinet/clients` отдают `specialty`
вошедшего; фронт `applyTabProfile()` скрывает «Протокол»/«Анализы» всем, кроме нутрициолога
(`hiddenTabsFor` — легко настроить per-профиль). Нутрициолог/владелец видят всё.

**Кабинет как продукт — ГОТОВ** (платформа + 12 языков + PDF + PWA + вид под профиль).
Опц. полировка: RTL для иврита; PNG-иконки для iOS.
**Приложение пациента (ИП) — Capacitor-обёртка** ← В РАБОТЕ. Папка `app/` (изолирована, на деплой
сайта не влияет). Спека — [[project_mobile_app]] / `docs/MOBILE-APP-MODEL.md`; гайд — `app/README.md`.
- ✅ **Слой 0 — скаффолд** (2026-06-08): `app/package.json` + `capacitor.config.json`
  (`appId=com.viael.interpreter`, `server.url`→живой ИП) + `www/` фолбэк + `.gitignore` (ios/android
  генерятся локально). Сборку/запуск делает владелец на Mac: `cd app && npm install && npx cap add ios android && npx cap open ios`.
- ✅ **HealthKit (iOS) — РАБОТАЕТ на устройстве (2026-06-10).** Это и есть путь Oura (web-API закрыт,
  PAT Oura убрал из обычных аккаунтов): кольцо → Oura app → Apple Health → наш мост. Авторизация
  запрашивает HRV+VO2 (подтверждено на телефоне Marina), данные импортируются и применяются.
  Мост `interpreter/healthkit-bridge.js` (HRV/пульс/сон/deep/VO2 → `m-*` → `applyManual()`), кнопка
  «📲 Apple Health» теперь во ВСЕХ платных (pro/pro-expert/elite, видна только в app).
  **Плагин: `@perfood/capacitor-healthkit` 1.3.2 + патч** (`app/patches/`, `postinstall:patch-package`):
  alpha.2 не читал HRV/VO2 — откатили на 1.3.2 и допатчили Swift (HRV→ms, VO2→mL/kg·min, авторизация
  под именами sample). Сборка: `npm i --legacy-peer-deps`, `LANG=en_US.UTF-8 npx cap sync ios`.
  ⏳ Хвост: сверить реальные ЦИФРЫ (что заполнилось не пусто) на ночных данных кольца; источник
  помечается «Apple-manual» (косметика).
  ✅ **Гоча Info.plist решена (2026-06-11):** `ios/` в .gitignore → при пересоздании терялись
  `NSHealth*`-ключи и HealthKit-entitlement. Скрипт `app/scripts/setup-ios.js` (`npm run setup:ios`,
  авто после `npm run add:ios`) идемпотентно их восстанавливает. См. [[project_app_iap_plan]].
- ✅ **App-вид / UX (2026-06-09, общий `interpreter/app-mode.js`):** строгие переходы шагов
  (мгновенно, к верхнему краю), авто-фиксация коротких шагов (влезает → не скроллится), hero только
  на входе (step0), футер скрыт, плотные отступы, верхний отступ под компактный логотип, логотип =
  декор/«домой», таб «Сегодня» → «Главная» (12 яз), скрыты внешние Hotmart-ссылки (anti-steering).
  Паритет во всех 4 инструментах (vio/pro/pro-expert/elite).
- ✅/⏳ **«Импорт из устройства» — Ultrahuman:** карточка с ручным вводом добавлена во все 4
  инструмента (vio/pro/pro-expert/elite) + `desc_ultrahuman` ×12 языков (2026-06-11). Осталось:
  вкладка «🔗 Подключить» (OAuth) — когда придут Client ID/Secret от партнёрского трека Ultrahuman.
- ⏳ **IAP для PRO** — встроить Apple In-App Purchase (план — [[project_app_iap_plan]]).
- 🟡 **Health Connect (Android) — КОД ГОТОВ, ждёт сборки/проверки на устройстве (2026-06-17).**
  Готовых плагинов под наши метрики нет (все требуют Cap7 + ни один не отдаёт HRV) → сделан
  СВОЙ локальный плагин `app/plugins/health-connect` (`@viael/health-connect`, Kotlin на
  `androidx.health.connect`): читает HRV(RMSSD)/пульс покоя/VO2/SpO₂/сон-стадии/шаги → нормализует
  в нативе. JS-мост `interpreter/healthconnect-bridge.js` (зеркало healthkit-bridge), кнопка `#hc-btn`
  «⌚ Health Connect» в карточке импорта (видна только в Android-app, как Apple-кнопка). Зависимость
  `file:plugins/health-connect` в `app/package.json`. **Ручные шаги владельца** (minSdk≥26, activity-
  обоснование прав, провайдер HC) — `app/plugins/health-connect/README.md`. ⚠️ tempDev в HC нет.
  ⏳ Проверить на Android (у владельца Huawei с Play Market): пишет ли его Fitbit HRV/сон в Health Connect.
- ⏳ Push, проверка экрана «Мой специалист» в обёртке, публикация.
- Нужно от владельца: Node+Xcode+Android Studio на Mac, аккаунты Apple ($99/год) / Google ($25).
- Потом: PWA (значок на экран, телефон/планшет — ВОПРОС владельца); RTL для иврита; вид под
  профиль (не-нутрициолог). Кабинет специалиста = веб-PWA; магазинное app — это ИП пациента.
- Контекст: модель работает и офлайн (местные специалисты), и **онлайн** (специалисты по миру,
  как владелец в EXPERT/ELITE — там перевод/12 языков становятся ядром, см. [[project_platform_specialists]]).

Остатки (не блокеры, по готовности):
- **Онлайн-модель, канал 1 «специалист приводит сам»** (спека `docs/PLATFORM-MODEL.md` §10):
  - ✅ **Блок «Моя ссылка для приглашения»** в кабинете (топбар «🔗 Моя ссылка»): ссылка + QR +
    копирование + тёплый шаблон сообщения (12 яз). `/cabinet-auth` и `/cabinet/clients` отдают
    `ref_code`+`name` вошедшего. (2026-06-07)
  - ✅ **Реф-ссылка → анкета** `book/anketa/?ref=<код>`: анкета шлёт `ref`, де-брендит шапку
    (прячет VIA-L-логотип/тег, ставит имя специалиста через `/specialist-by-ref`); `/anketa-submit`
    при `ref` привязывает пациента к специалисту (`specialist_id`+согласие, `deliverAnketaRef`),
    минуя care-группу основателя. Тем самым **Анкета расцеплена от покупки** EXPERT/ELITE. (2026-06-07)
  - ✅ **Индикатор данных** в списке и шапке карточки (`dataBadge`): зелёный «Данные ✓» при
    наличии ИП-разборов/биометрии, серый «Ждёт данных» для привязанных без данных, ничего —
    у обычных платных клиентов основателя (12 яз). (2026-06-07)
  - ✅ **GDPR-текст согласия в анкете при `?ref`** (`consentText()`): подставляет имя специалиста
    вместо бренда «Vía-L» (единый токен во всех 12 переводах) — согласие «на того». (2026-06-07)
  - При желании позже: запоминание `ref` (localStorage) на всём пути + «рекламный» лендинг
    (вместо прямого захода на анкету); ручной ввод кода как запасной путь; перепривязка, если
    пациент уже у другого специалиста (край §3).
- **ИИ-советник (один движок, поведение по тарифу)** — спека `docs/ADVISOR-MODEL.md`.
  - ✅ **Бэкенд `/advisor-chat`** (2026-06-07): Claude + грунтовка `faqMatch`, he/ar/ja/ko→Sonnet,
    гайдрейлы без диагнозов/обещаний/цен; tier-aware (self-serve отвечает, human-led → к специалисту).
  - ✅ **Советник в ИП** (2026-06-07): чат-виджет на 12 языков (читает `vial_lang`, RTL для he) на
    `interpreter/index.html` (лендинг), `interpreter-vio.html` (`tier=vio`), `interpreter-via-l.html`
    (`tier=pro`). На **сайте (`index.html`) советник УБРАН** — решение владельца 2026-06-07 (только
    в ИП). EXPERT/ELITE-страницы — **БЕЗ советника намеренно** (там ведёт человек). Значок —
    `interpreter/Logo/logo-ai-v2.png` (прозрачный, 256²; имя с `-v2` для сброса мобильного кэша).
  - ✅ **Слой 3 — human-led для привязанных** (2026-06-08, коммит `c9874ad`): `interpreter-via-l.html`
    читает `localStorage.vial_spec_link` (ставит `my-specialist.html` при подключении) → если пациент
    привязан к специалисту, виджет шлёт `tier:'specialist'` → бэкенд уходит в human-led; приветствие
    `greetLed` с именем специалиста (12 яз). Обычный PRO не тронут. EXPERT/ELITE-страницы по-прежнему
    без советника. ⏳ остаток: при желании то же определение привязки на vio/лендинге (минор).
- **Юридические документы (к юристу ЕС; реализация — не код, а тексты + click-accept).** Спека —
  `docs/PLATFORM-MODEL.md` §10 «Открытые вопросы». Три слоя: (1) пациент↔специалист — согласие на
  данные ✅ есть + шаблон договора/информ-согласия на консультацию (ответственность специалиста);
  (2) пациент↔мы — Privacy Policy + Условия использования (PRO), явно про передачу данных
  специалисту; (3) мы↔специалист — договор доступа к платформе + **DPA** (специалист=контролёр,
  мы=обработчик), click-accept при первом входе. По пациентам ничего не подписываем. Вычитка юристом.
- ✅ **Шаг 2, хвост — СДЕЛАН (2026-06-08):** веб-экран пациента «Мой специалист»
  `interpreter/my-specialist.html` (12 языков, RTL для he): превью специалиста по коду
  (`/specialist-by-ref`), GDPR-согласие на имя специалиста, привязка через
  `/specialist/connect`, состояние «подключено» + отвязка (`/specialist/unlink`), статус в
  `localStorage.vial_spec_link`. Вход «🩺 Мой специалист» — в топбаре 4 интерпретаторов и
  меню лендинга ИП (ключ во всех 12 языках). Это же — первый экран будущего app.
- **Шаг 4, конфиг владельца:** создать в Hotmart продукт-подписку «доступ к платформе» и повесить
  её вебхук на `/specialist-access-webhook` (внутренний эффект вебхука вживую не гонялся — нужен
  реальный Hotmart-токен). До тех пор абонплата управляется вручную в панели.
- При желании: профильные виды кабинета (урезанные вкладки для гинеколога) — спека §9.

---

## 3. FAQ-память — остаток (необязательный)

База `faq:db` засеяна (409 пар, в проде). Остаток:
- Вычитка профильным специалистом постфактум по живым диалогам.
- Опц.: оптимизация `faqMatch` (префильтр по ключевым словам перед Haiku).
- Идея на будущее: автоответ ВВЕРХУ воронки (бесплатный ИП), но НЕ в платном ведении.

---

## 4. Hotmart — юридическая упаковка EXPERT/ELITE (EU MDR / FTC / FDA) 🔴 критично

> Владелец 2026-07-02: увод EXPERT/ELITE на Hotmart НЕ защищает от регуляторов ЕС/США.
> Hotmart не модерирует тексты строго, НО: **FTC** штрафует за «Health Claims» (обещания при
> цистите/остеопорозе/геморрое требуют клиники уровня фармы); **FDA** может признать софт
> «незарегистрированным медизделием» (SaMD); **EU MDR 2017/745** — любой продукт, заявляющий, что
> «диагностирует/предотвращает/лечит» болезнь, автоматически под медрегулирование; **сам Hotmart**
> (General Policy 4.11) банит продукты-«альтернативу медпомощи»/обещания излечения → по жалобе из
> ЕС/США мгновенная блокировка аккаунта + заморозка баланса. Глубокий контент резать НЕ надо —
> переупаковать «мы лечим болезни» → «учим управлять телом/биоритмами/образом жизни».
>
> **📋 Готовое задание с копипаст-текстами (дисклеймеры 5 языков, таблица замен заголовков,
> чек-лист) — `docs/HOTMART-COMPLIANCE-BRIEF.md`.**

- [ ] **Принцип 1 — статус авторов/кураторов: «наставник», не «врач».** Позиционировать как
      health-coach / нутрициолог / консультант по образу жизни (сертификат Perry Academy =
      квалификация в lifestyle-поддержке, не в медицине). Проверить лендинги/био.
- [ ] **Принцип 2 — смягчить заголовки лендинга Hotmart.** ❌ «Программа лечения рецидивов цистита и
      нормализации гормонов» → 🌿 «Углублённая программа восстановления интимного баланса и управления
      внутренними ресурсами». Пройти все заголовки/оферы EXPERT/ELITE.
- [ ] **Принцип 3 — жёсткий Medical Disclaimer крупно** на: (а) странице оплаты Hotmart, (б) лендинге,
      (в) внутри кабинета EXPERT/ELITE. Текст (EN + язык пользователя):
      «This program is for educational and wellness purposes only. It does not constitute medical
      advice, diagnosis, or treatment. Always consult with a qualified healthcare professional before
      making any changes to your health regimen».
- [x] **Код-часть (воркер, 2026-07-02):** AI-вывод EXPERT/ELITE получил compliance-рамку —
      клинический/образовательный язык разрешён, но запрещены claims «лечит/излечивает/предотвращает
      болезнь», + обязательный образовательный дисклеймер в конце. См. `cloudflare-worker.js`
      (`wellnessGuard` else-ветка). Память — [[feedback-wellness-terminology-mandatory]].
- [ ] **Распространить выходной AI-guardrail на остальные генерации** (после smoke-теста разбора).
      Внедрено (2026-07-10) только на главном разборе `/analyze` для велнес VIO/PRO: `AI_RISK_RE`
      (Filter 1) → self-check (Haiku, только при risk) → мягкая перегенерация → нейтральный фолбэк.
      **НЕ обёрнуты:** (1) «Памятка дня» `/day-plan` (JSON, VIO/PRO); (2) EXPERT/ELITE-черновик
      (Hotmart-путь). Решить после живых smoke-тестов, нужно ли распространять архитектуру туда;
      для EXPERT банлист/self-check-вопросы адаптировать под клинический-разрешённый тон (запрещены
      claims о лечении, а не сами клин.термины). См. `cloudflare-worker.js` (`wellnessGuardrail`).

---

## Правила работы (важно)

- Одно конкретное предложение прозой, без опросников. Короткие ответы.
- **Approval per-file** — не переносить паттерн с тарифа на тариф без «ок».
- **«Аккуратненько»:** бэкап + синтаксис-чек inline-JS (`node vm.Script`) ДО коммита.
  Паттерн: правка pro → чек → ретест Игорем на живом → перенос в pro-expert/elite.
- **Деплой:** HTML → GitHub Pages (`git push`, домен **via-l.com**); воркер →
  `cd interpreter && wrangler deploy`. Секреты — `wrangler secret put` (репо публичный).
- Папки `Infa Cloude/N-*` — провенанс (gitignore), не коммитить.

---

## PRO — на завтра (сессия 2026‑07‑19)

- [ ] **Фигура в разборе на мобильном.** Перешли на масштаб по высоте блока
  (`background-size:auto 100%`, привязка `top/bottom`). Правило `@media(max-width:820px)`
  (`width:38%;max-width:180px`) в вебе смотрится нормально — владелец проверит в нативке.
- [ ] **Вычитка весов индекса специалистом** → [`docs/INDEX-SCORING-REVIEW.md`](../docs/INDEX-SCORING-REVIEW.md):
  33 правила с весами и условиями. Нужен вердикт по каждому (ОК / изменить / убрать)
  + ответы: адекватна ли стартовая точка 70; нет ли перекоса (сон −28, гормоны −29);
  что пропущено. **После утверждения** можно показывать клиенту точную разбивку «−N»
  вместо нынешнего осторожного «Учтено в индексе».
- [ ] **ИИ-разбор — проверить после пополнения баланса API.** Клиент и воркер уже в проде:
  в payload уходят `source` и `source_changed`, промпт различает «прибор vs со слов»
  и не сравнивает напрямую при смене источника. Убедиться, что разбор это учитывает.

### Сделано 2026‑07‑19 (для контекста)
Шапка разбора как у Oura (заголовок+дата в топбар), лента дней сжата; хвост вычищен
(убраны «завершил анализ / использовано данных», дубль дисклеймера, апсейл) → блок
«Ваш анализ завершён»; все большие кнопки сведены к `.btn.btn-primary`; `.cta-card`
и гейты переведены на холодный градиент; фигура заменена и привязана по высоте;
**честность показателей** (стресс/температура словом + «по вашей оценке», температура
считается от реального `tempDev`, «HRV к норме возраста»), **честность индекса**
(«композит по вашим данным, не измерение» + «Учтено в индексе»), медицинская рамка
триажа убрана («Консультация с врачом» → «На что обратить внимание»);
**i18n-долг закрыт полностью** — было 133 неполных `L()`-объекта, стало 0.
