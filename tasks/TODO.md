# TODO — единый список открытых задач

> **Единственный живой список задач проекта.** Закрытую задачу — УДАЛЯЙ отсюда,
> а факт выполнения коротко записывай в архитектуру (root `ARCHITECTURE.md` §9
> «Журнал» + детальная механика в `interpreter/ARCHITECTURE.md` соответствующего §).
> Так этот файл всегда = только то, что ещё не сделано.
>
> **Полная чистка 2026-08-03:** убраны разделы-история (VIO/PRO-архив, «Главная ИП»,
> «Сделано 2026-07-19», отчёты о готовых этапах кабинета и локализации) и устаревшие
> имена тарифов. Осталось 29 живых пунктов, сгруппированных по продукту, а не по датам.
>
> **Имена продуктов (канон, [[project_tariff_naming_canon]]):** **VIA-L** — приложение для
> App Store (`interpreter-via-l.html`); **VIA-L EXPERT** — ведение специалистом
> (`interpreter-via-l-expert.html`, PWA вне стора). Тарифов VIO/PRO/ELITE больше нет —
> в коде остались только легаси-пломбы (`pro`/`elite` в payload, `ELITE-8W` в базе кодов),
> их не переименовывать. Контекст по любому пункту — `ARCHITECTURE.md` (root),
> `interpreter/ARCHITECTURE.md` (§15–18) и memory.

---

## ⚖️ ПЛАТФОРМА: правовой контур и защита клиента (план от 2026-08-03) ← ИДЁМ ПО ПОРЯДКУ

> Собрано из разговора с владельцем 2026-08-03. Модель: **специалисты — не работники VIA-L**,
> деньги клиента идут напрямую специалисту (платёжной интеграции у нас нет и не будет —
> это и защищает: платформа не сторона клиентского договора). Отсюда всё ниже.
> ⚠️ Юрист обязателен для текстов и для раздела «роли по данным» (контролёр/обработчик);
> форма (ФОП/ТОВ/ЕС) пока не создана. Паспортные данные НЕ храним — достаточно ФОП/налогового
> номера (открытый реестр) + адрес для корреспонденции.

### A. Правовой контур (защита от претензий)
- [x] **A1. ✅ Оговорка об ответственности в трёх местах:** оферта специалиста, письмо с кодом,
      экран «Мой наставник» в приложении. Текст: «Ведение оказывает [ФИО]; он несёт
      ответственность за консультации. VIA-L предоставляет программу и стороной договора
      не является». + прямо: деньги идут специалисту, платформа их не держит и не возвращает.
- [x] **A2. ✅ Акцепт оферты до оплаты + лог** (текст оферты — заглушка до юриста, версия `offer-v1`). Галочка «ознакомлен с условиями» рядом с кнопкой
      «Начать ведение»; лог: дата, время, версия текста оферты, id специалиста, устройство.
      Этот лог = доказательство (полноценная ЭЦП для €390–500 избыточна, режет конверсию).
- [x] **A3. ✅ Проверка специалиста ДО публикации.** Поля в карточке: публичные (образование,
      квалификация, номер регистрации/ФОП, сайт, город, языки) и служебные, видны только
      владельцу (юр. ФИО, налоговый номер, адрес для корреспонденции, телефон, дата и способ
      проверки, ссылка на подписанный договор). Галочка согласия на публикацию с датой.
      **Гейт:** не проверен → не публикуется в витрине и НЕ может выдавать доступы.
- [x] **A4. ✅ Бейдж «данные проверены платформой» + дата** на карточке в витрине.
- [x] **A5. ✅ Кнопка жалобы в один клик** из приложения клиента → владельцу (сигнал за минуту,
      а не через отзывы в интернете). Механика отключения уже есть.
- [x] **A6. ✅ Витрина: раскрытие подробностей по кнопке** (как просил
      владелец: клик открывает страницу, второй клик разворачивает детали).

### B. Непрерывность для клиента (специалист пропал)
- [x] **B1. ✅ Режим чтения после окончания срока.** Сейчас гейт закрывает интерфейс целиком:
      данные на устройстве целы, но недостижимы. Нужно: история и тренды доступны, новых
      разборов нет.
- [x] **B2. ✅ «Скачать мои данные» на экране «срок истёк»** (предупреждение за 7 дней — остаток) (право на переносимость) +
      предупреждение за 7 дней до конца прямо в приложении.
- [x] **B3. ✅ Передача клиента другому специалисту** — кнопка у владельца, срок сохраняется.
      Сейчас передать нечем, только правкой в базе.

### C. Дисциплина платежей (без вреда клиентам)
- [x] **C1. ✅ Ступенчатая блокировка вместо рубильника:** льготное окно 7–14 дней → запрет
      выдавать НОВЫЕ доступы (бьёт по источнику долга) → при глубокой просрочке режим чтения
      → `disabled` только вручную. Клиенты во всех случаях доигрывают оплаченный срок.
- [x] **C2. ✅ Причина отказа при входе.** Сейчас `lapsed` даёт «неверный пароль» — человек не
      понимает, что дело в оплате. Возвращать reason + сумму к оплате.
- [x] **C3. ✅ Квота-предохранитель (лимит неоплаченного софта, по умолчанию €100):** нельзя выдать доступов больше, чем оплачено/разрешено —
      ограничивает максимальный убыток при исчезновении специалиста (механизм квот есть).

### D. Гигиена доступа и доказательства
- [ ] **D1. Кнопка «Сгенерировать» пароль** в карточке специалиста (сильный, показать один раз).
- [ ] **D2. Смена пароля самим специалистом** — после неё владелец пароля не знает, и «под его
      учёткой работал кто-то другой» перестаёт быть возможным. Позже: приглашение по ссылке.
- [x] **D3. Акт-выгрузка по клиенту:** когда доступ выдан, сколько дней, сколько разборов
      использовано, была ли переписка. Данные уже логируются — нужна выгрузка.

### E. Тексты (черновики мои, подписывает юрист)
- [x] **E1. Оферта клиент ↔ специалист** — черновик `docs/OFFER-CLIENT-SPECIALIST-DRAFT.md`
      (версия `offer-v1`, трёхсторонняя схема, мед-дисклеймер, возвраты, ограничение ответственности).
      Чужой шаблон (thebody.school) не подошёл — взят только скелет разделов. Подписывает юрист.
      Пересобран 2026-08-03 в рамочную оферту маркетплейса + Приложение №1; поля `country`/`legal_form`
      специалиста заведены и показаны на витрине.
- [x] **E2. Договор платформа ↔ специалист** — черновик `docs/AGREEMENT-PLATFORM-SPECIALIST-DRAFT.md`:
      лицензия на КАБИНЕТ + право выдавать EXPERT, €30/мес абонплата и €20/мес софт за клиента,
      стадии неплатежа, indemnity, «не работник платформы», клиентские доступы не отключаются,
      DPA-раздел (контролёр/обработчик + двойная роль). Подписывает юрист.
- [x] **E3. Политика данных** — черновик `docs/DATA-POLICY-CLIENT-DRAFT.md`: два режима хранения
      (VIA-L device-only vs EXPERT + карточка), роли включая joint controller по ИИ, сроки
      (здоровье 90 дней после ведения, факт услуги 3 года), права + кнопки в приложении, US-блок.
- [ ] **E3-хвост:** ✅ автоудаление 90 дней (`eraseExpiredHealthData`, cron), ✅ политики разведены
      (VIA-L→legal-app, EXPERT→legal + раздел 7-бис ×12), ✅ ссылка под кнопкой удаления.
      Осталось:       разделить `legal-app/privacy.html` (device-only) и `legal/privacy.html` (EXPERT);
      ссылка на политику из EXPERT и из кнопки удаления; перевод EN→UK/ES/RU.
- [x] **E4. Вопросы юристу** — `docs/LEGAL-QUESTIONS.md`: 20 вопросов пятью блоками + готовый
      блок «Контекст» для консультации. Юриста у владельца пока нет — список рабочий, не закрытый.
      Блокируют запуск: 1, 2, 4, 8, 10, 13, 14.

---

## 🔥 Ближайшее — хвосты сессии 2026-08-01/03

Контекст: журнал `ARCHITECTURE.md` §9 (записи 2026-08-01/03); память
[[project_supplements_module]], [[project_prompt_rules_placement]], [[project_token_economics]].
Сырьё OpenEvidence N-11 — `interpreter/Infa Cloude/E` (⚠️ НЕ в `Q`), третий круг —
`E/К. Вопросы третьего круга` (⚠️ ответ на вопрос 40 лежит в файле `49.txt`).

- [ ] **Блок анализов глазами** (владелец, живой превью): сетка полей на узком экране, поле даты
      в iOS, 12 языков. Собран, но визуально ни разу не просмотрен.
- [ ] **Через неделю по `ai_usage`:** остаточные триггеры guardrail по колонке `note`; решить
      судьбу `cache_control` (платим надбавку за запись 25% и НИ РАЗУ не читаем — `cache_r=0`).
- [ ] **Первое закрытие петли эксперимента** у живого клиента — через 7 дней после первого
      недельного разбора; посмотреть, что человек реально увидит.
- [ ] **Глубокий сон в индексе — доделать честность до конца (обсудить).** Текст теперь
      оговаривает ненадёжность фаз, вопрос при синке снят, штраф в плитке смягчён. Осталось решить,
      не пора ли пересмотреть и вклад субъективного ответа о фазе в сам балл (сейчас в индекс он
      не входит вовсе — только в плитку).
- [ ] **Абонплата своему специалисту** (владелец): очистить `platform_since` у специалиста №1,
      если платформой его не облагаем — поле теперь есть в карточке.

---

## 📱 VIA-L — выпуск в App Store

Приложение собрано и запускалось на устройстве владельца; остаток — сборка, стор и
данные клиента. Канон: [[project_app_iap_plan]], [[project_app_sim_build]],
[[feedback_via_l_appstore_isolation]]. Обоснование именований для ревью —
`docs/APP-REVIEW-LAB-NAMING-RATIONALE.md`.

- [ ] `cd app && npx cap sync` → сборка в Xcode, залить билд.
      ⚠️ известный блокер окружения: `pod install` падает (Ruby 4.0.5 + CocoaPods 1.16.2,
      `Encoding::CompatibilityError`). Обход: `cd app/ios/App && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 pod install`
      (нужен только при смене нативных плагинов; веб-контент копируется и без него).
- [ ] App Store Connect: auto-renewable подписка €15/мес → привязать к RevenueCat
      (entitlement `via_l_pro`, bundle `com.viael.interpreter`).
- [ ] App Privacy nutrition labels: задекларировать HealthKit-данные (не для рекламы/трекинга).
- [ ] Метаданные/скриншоты без мед-обещаний + демо-доступ ревьюеру (обход IAP-гейта, sandbox-аккаунт).
- [ ] Подтвердить корректность `ITSAppUsesNonExemptEncryption=false` (только стандартное шифрование).
- [ ] **Legal:** вычитка носителями 11 языков + хостинг `legal-app/` на чистом лендинге → URL в App Store Connect.
- [ ] **Долговременное хранение данных клиента — Уровень 1–2.** Сейчас всё в localStorage WebView (хрупко).
      ✅ Есть: бэкап export/import (Ур.3), cap метрик `*_daily` 1500 (≈4 г) / `*_ai_daily` 730 (≈2 г).
      🔴 Открыто: Ур.1 (нативный стор через Capacitor) + Ур.2 (iCloud/Google Drive + legal-согласие). [[project-storage-durability]]
- [ ] **Прогон на реальном устройстве:** карточка, авто-язык на iPhone, кэши день→день, RTL иврит,
      Capacitor-сборка, шифрование round-trip, советник (ответы по данным клиента + велнес-гардрейл).
- [ ] опц.: глубокий фолбэк «Памятки дня» на 12 языков (сейчас EN — App-Store-safe по решению владельца).
- [ ] **Фигура в разборе на мобильном.** Перешли на масштаб по высоте блока
  (`background-size:auto 100%`, привязка `top/bottom`). Правило `@media(max-width:820px)`
  (`width:38%;max-width:180px`) в вебе смотрится нормально — владелец проверит в нативке.

---

## 🧭 Воронка: VIA-L → витрина наставников → VIA-L EXPERT

Модель согласована с владельцем (2026-07-26/29). Канон — [[project_showcase_expert_funnel]],
`docs/PLATFORM-MODEL.md`. Кабинет специалиста и платформа как продукт описаны в
`docs/CABINET-MODEL.md` и `docs/PLATFORM-MODEL.md` — этапы 1–5 и локализация на 12 языков
закрыты и в проде, активных задач по ним нет.

- [ ] **Экономика — МОДЕЛЬ УТВЕРЖДЕНА 2026-07-29 (путь 1: доверие+счёт).** Потоки: клиент платит
      специалисту одну сумму (напр. €300 ведение 2 мес + €40 софт = €20/мес×2 = €340); специалист
      оставляет €300, нам переводит **на карту €40** (софт клиента) **+ €30/мес** за платформу.
      €30 — чистая абонплата за Кабинет, НЕ на токены. Токены покрывает €20/мес-софт, вшитый в цену
      специалиста. **Ограничители стоимости:** (а) EXPERT, как VIA-L, = **1 разбор/день/клиент** (клиент
      не может гонять бесчисленные проходы); (б) **квота разборов на каждый grant** (потолок нашей
      стоимости; механизм есть — `_planMax`/`expert_used`/`expert_max`). Себестоимость ≈€3/клиент/мес → €20
      даёт маржу ~€17. **Enforcement = доверие+счёт:** `/cabinet/expert-grant` при выдаче пишет сумму к оплате
      (срок×€20); Кабинет ведёт счёт специалиста «к оплате: €30 платформа + €N за выданные доступы» → платит
      на карту. Предоплаченный баланс — на потом, при росте. Никакой платёжной интеграции для клиентов
      (ни Apple IAP, ни веб-подписки) — специалист собирает всё сам.
- [ ] **Нативка VIA-L → синхро данных в Кабинет (будущая развилка, НЕ сейчас).** Compliance = функция кода,
      что шлётся в `/analyze`: нативка шлёт обычный код → велнес-разбор без мед-терминов (прошла модерацию).
      Можно добавить отправку СЫРЫХ данных в Кабинет (Apple не ревьюит серверную синхро к провайдеру), НО
      нативка обязана продолжать ПОКАЗЫВАТЬ пользователю только велнес; клинический разбор генерится/виден
      ВНЕ App Store (Кабинет спеца + EXPERT PWA). Больший объём + касание нативки → откладываем осознанно.
- [ ] **Оплата EXPERT: Hotmart → LiqPay (ПриватБанк).** Зафиксировано 2026-07-28.
      Разделение продуктов: **VIA-L** (глобал, App Store) остаётся на Apple IAP;
      **VIA-L EXPERT** продаётся с сайта нутрициолога (UA/RU + ЕС, разовый платёж
      за программу 8W/12W) → переводим на LiqPay. **Блокер — не код, а ФОП:** без
      украинского ФОП/ТОВ не выдадут merchant-аккаунт и ключи `public_key`/
      `private_key`. Пока сайт не афишируется — спешки нет, Hotmart стоит как есть.
      **Решение (разведка сделана):** цепочка выдачи уже разделена. Вход от
      Hotmart ([handleHotmartWebhook](../interpreter/cloudflare-worker.js#L969))
      только проверяет токен и разбирает *его* payload; всё полезное — в
      [handleInterpreterPurchase](../interpreter/cloudflare-worker.js#L1067):
      берёт FREE-код из Sheets (`assign_code`), TG-карточка, связка `код→клиент` в
      KV, карточка в кабинете, **письмо клиенту с кодом на 12 языках**
      ([buildInterpreterEmailBody](../interpreter/cloudflare-worker.js#L1148)) —
      ни строчки про Hotmart. **Переезд = дописать только «вход»:** новый роут
      `handleLiqpayCallback` (проверка подписи `base64(sha1(private_key+data+
      private_key))`, статус `success`, продукт из заказа) → вызвать ту же
      `handleInterpreterPurchase(...)`. **Коды уйдут автоматически, вручную слать
      не надо** (нужен лишь запас FREE-кодов `VIAL-EXPERT-8W/12W` в Sheets через
      `code-generator.html`). «Обрезать Hotmart» потом = удалить `/hotmart-webhook`,
      `handleHotmartWebhook`, `detectLangFromHotmart`; ядро не трогаем. Объём после
      получения ключей — ~полдня. См. memory [[project_tariff_merge_via_l_base]].
- [ ] **Экран «Мой наставник» без кода = тупик (VIA-L).** → покрыто пунктом «Витрина
      специалистов» выше (§ Воронка VIA-L → витрина → EXPERT PWA). Трёхслойность: 1) `?ref`
      → код подставлен; 2) ручной ввод; 3) кода нет → витрина. См. PLATFORM-MODEL §10 №3.
- [ ] **Класс B — внутренние id (опц., не срочно).** `MAX`/`tier:'pro'`/лоу-кейс
      `'elite'`/`'pro'` (флаги режима ИИ в /analyze) + ~40 ELITE-комментариев. НЕ ломает
      ничего; чистить только пачкой при желании нулевых вхождений. См. [[project_tariff_naming_canon]].

---

## 📊 Индекс дня, база знаний, новые модули

- [ ] **Живая проверка** текста разбора: без «норма для возраста»/«нехватка
      магния»; заметка «простужен» учитывается. (SpO₂-часть закрыта.)
- [ ] **Вычитка весов индекса профильным специалистом** → [`docs/INDEX-SCORING-REVIEW.md`](../docs/INDEX-SCORING-REVIEW.md):
      33 правила с весами и условиями + таблица весов §8. Нужен вердикт по каждому
      (ОК / изменить / убрать) и ответы на три вопроса: адекватна ли стартовая точка 70;
      нет ли перекоса (сон −28, гормоны −29); что пропущено — в т.ч. «второе дыхание» и
      «низкая температура + усталость». Ссылки проверены веб-поиском, выдуманных нет.
      MRS реализовывать нельзя (лицензия ZEG Berlin), ссылаться — можно; AMS свободна
      (`docs/INDEX-SCORING-SOURCES.md` §1.1). **После утверждения** можно показывать клиенту
      точную разбивку «−N» вместо нынешнего осторожного «Учтено в индексе».
- [ ] **Цикл-карта симптомов настроения (аналог DRSP).** Мы уже собираем и фазу цикла, и
      настроение/тревогу — можем строить карту симптомов по циклу и дескриптивно подсвечивать
      «симптомы весь цикл, а не только перед месячными → обсудите с врачом» (различение
      ПМДР / предменструальное обострение / первичное расстройство). ⚠️ **Стопор:** сейчас
      настроение/тревога/стресс идут НЕДЕЛЬНО/двухнедельно (канон ритма), а для настоящей
      DRSP нужна ДНЕВНАЯ привязка к фазе ≥2 циклов. Значит: либо повысить каденс сбора
      настроения (мин. дневная отметка с тегом фазы), либо ограничиться грубым недельным
      паттерном. Требует решения по каденсу + UI карты. Только дескриптивно, без диагноза.
- [ ] **ОАС-скрин-модуль (осведомлённость + мягкий флаг).** ОАС у женщин среднего возраста
      сильно недодиагностирован, растёт в менопаузе, лечится; фенотип — бессонница/усталость/
      настроение, не храп. Модуль: риск-факторы, что клиент опознаёт сам (храп, ноктурия,
      утренние головные боли, гипертония, ИМТ>30, ранняя/хирургическая менопауза) + носимые-
      сигналы (высокий ночной пульс, частые пробуждения, низкий SpO₂) → «обсудите со
      специалистом про исследование сна», НЕ диагноз. ⚠️ Новых входных полей (храп/ноктурия/
      апноэ) сейчас нет — нужно добавить в опросник. STOP-опросник недооценивает женщин
      (пол-пункт=0) → порог ниже.

---

## ⌚ Носимые устройства

Каркас единый (Worker `/<vendor>/start|callback|metrics`, KV `WEARABLE_TOKENS`,
фронт `connectWearable`/`fetchWearableLive`); детали — `interpreter/ARCHITECTURE.md` §15–16.
В проде: Oura, Fitbit, Polar, Withings. Статус и заблокированные вендоры —
[[project_wearable_integrations_status]], стратегия сбора — [[project_import_panel_modes]].

- [ ] **📨 Запросы на affiliate-скидку для клиентов — Oura + Ultrahuman (✅ ОТПРАВЛЕНЫ 2026-07-29, ждём ответа).**
      Слал с `integration@via-l.com`. **Oura** → форма «Oura for Organizations» (объём «5-19», Health & Wellness,
      цель = скидка клиентам + мягко API); API-трек у Oura отдельный (партнёрство >10 отклонено, Becky #6994484).
      **Ultrahuman** → partnership-форма: (1) партнёрский код скидки клиентам, (2) API-доступ (`ring_data`/`cgm_data`/
      `profile`). ⚠️ **affiliate ≠ API-коды** — разные команды. Тексты (EN+RU) — в диалоге 2026-07-29. ⏳ Дождаться
      ответов; если скидки идут через отдельную affiliate-программу — попросить переключить туда.

---

## ⚖️ Юридическая упаковка (EU MDR / FTC / Hotmart)

> Рамка регуляторов: **FTC** — health claims; **FDA** — SaMD; **EU MDR 2017/745** —
> «диагностирует/предотвращает/лечит» переводит продукт в медизделие; Hotmart банит
> «альтернативу медпомощи». Копипаст-тексты дисклеймеров и таблица замен —
> `docs/HOTMART-COMPLIANCE-BRIEF.md`.
>
> **Аудит лендингов 2026-08-03:** заголовков-обещаний нет ни на одной из пяти страниц
> (проверено по словам лечение/терапия/диагноз/гарантируем и по глаголам
> уберём/устраним/вернём/нормализуем — ноль совпадений). Футер-дисклеймер был зашит
> только по-английски на четырёх страницах программ — локализован на 4 языка.

- [ ] **Проверить страницу оплаты Hotmart и письма** — единственное место, где дисклеймер ещё не
      проверялся глазами. На самих лендингах он теперь есть везде и на языке читателя.
- [ ] **Статус авторов — «наставник», не «врач».** Пройти по био и подписям на лендингах и в
      витрине специалистов: позиционирование health-coach / нутрициолог / консультант по образу
      жизни. (Проверялось частично; отдельного аудита формулировок не было.)
- [ ] **Guardrail на остальные генерации — ПЕРЕСМОТРЕТЬ, а не «распространить».** Пункт писался,
      когда guardrail считался защитой. Живой замер 2026-08-02/03 показал обратную сторону: он
      переписывал 9 разборов из 10, треть стоимости уходила на переписывание, и срабатывал он на
      нашем же дисклеймере и на отрицаниях. После починки — 0 из 3. Прежде чем вешать его на
      `/day-plan`, посмотреть неделю данных в `ai_usage.note`: возможно, дешевле и честнее
      детерминированные фильтры (дозы, травы, коды), которые там уже стоят.

---

## 🔒 Канон ИИ-воркера — анти-регресс (не задача, а правило)

- **Заморожен compliance-фундамент** (не ослаблять без слова владельца): `temperature` 0.35,
  `AI_SAFETY_RULES`, детерминированные фильтры (`_stripHerbLines`, `_stripAnyDoses`,
  `_stripCodeTokens`, `wellnessHardScrub`).
- **Приоритет у детерминированного слоя, не у «естественности» текста.** Каждое правило
  стоит на конкретном дефекте; зелёный автоскан ≠ «всё ок» — периодически смотреть живой
  вывод ГЛАЗАМИ.
- **НО фильтр обязан ловить УТВЕРЖДЕНИЕ, а не слово** (уроки 2026-08-02/03): регекс на
  «диагноз» срабатывал на нашем же дисклеймере, на «болезнь» — в отрицании, на «означает» —
  в связке метрик клиента. Каузальные и медицинские маркеры гейтить объектом-диагнозом и
  проверять на отрицание. ⚠️ `\b` в JS не видит кириллицу — границы юникодные.
- **Де-медикализация не должна прятать данные, на которых стоит безопасность:** название
  анализа, органа и класса лекарства, о котором человек сообщил сам, — называем; прячем
  ДИАГНОЗ. Пословные подмены, которые нельзя согласовать по падежам, ретируем
  (так снята «воспаление» → «фоновая нагрузка»).
- **Само-триаж дефектов:** дезинформация = БЛОКЕР, чинить сразу; косметика — в очередь.
- Каноны терминологии и claim'ов — [[project_prompt_rules_placement]],
  [[feedback_via_l_appstore_isolation]].

---

## Правила работы (важно)

- Одно конкретное предложение прозой, без опросников. Короткие ответы.
- **Approval per-file** — не переносить паттерн с тарифа на тариф без «ок».
- **«Аккуратненько»:** синтаксис-чек inline-JS ДО коммита; при правке KB — сверять размеры
  блоков до и после (заменять содержимое можно только там, где это заглушка).
- **Деплой:** HTML → GitHub Pages (`git push`, домен **via-l.com**); воркер →
  `cd interpreter && wrangler deploy`. Секреты — `wrangler secret put` (репо публичный).
  ⚠️ Первый запрос сразу после деплоя может прийти на старую версию — проверять повтором.
- Папки `Infa Cloude/*` — провенанс (gitignore), не коммитить.
