# TODO — единый список открытых задач

> **Единственный живой список задач проекта.** Закрытую задачу — УДАЛЯЙ отсюда,
> а факт выполнения коротко записывай в архитектуру (root `ARCHITECTURE.md` §9
> «Журнал» + детальная механика в `interpreter/ARCHITECTURE.md` соответствующего §).
> Так этот файл всегда = только то, что ещё не сделано.
>
> **Полная чистка 2026-08-03:** убраны разделы-история (VIO/PRO-архив, «Главная ИП»,
> «Сделано 2026-07-19», отчёты о готовых этапах кабинета и локализации) и устаревшие
> имена тарифов. Осталось 29 живых пунктов, сгруппированных по продукту, а не по датам.
>
> **Имена продуктов (канон, [[project_tariff_naming_canon]]):** **VIA-L** — приложение для
> App Store (`interpreter-via-l.html`); **VIA-L EXPERT** — ведение специалистом
> (`interpreter-via-l-expert.html`, PWA вне стора). Тарифов VIO/PRO/ELITE больше нет —
> в коде остались только легаси-пломбы (`pro`/`elite` в payload, `ELITE-8W` в базе кодов),
> их не переименовывать. Контекст по любому пункту — `ARCHITECTURE.md` (root),
> `interpreter/ARCHITECTURE.md` (§15–18) и memory.

---

## 🇺🇦 Дорогие языки: Sonnet → Haiku + перевод (замер 2026-09-17)

- [x] **Код готов 2026-09-17 (не задеплоен):** `_GEN_VIA_RU=['uk']` + `_genLang()` в воркере — uk генерится по-русски
      на Haiku, перевод делает штатный `_enforceLang`/`_enforceLangPlan`. Попутно починен баг: для кириллической
      цели удачный украинский перевод считался провалом и клиенту уходил АНГЛИЙСКИЙ текст.
      `/weekly-report` и `/ai-memory` для uk оставлены на Sonnet (дёшевы: $0.03 и $0.009).
      После деплоя: живой украинский проход + сверка `ai_usage` (строка analyze с note `gen:ru` и строка `translate`).
- [ ] **Контекст замера:** `docs/UK-LANG-COST-TEST.md` — два украинских
      разбора на одном профиле (Sonnet напрямую $0.2186 против Haiku ru + перевод ≈ $0.113).
      Полный проход украинского $0.34 → ≈$0.16. Владелец одобрил перевод для uk 2026-09-17.
      he/ja/ko ПРОВЕРЕНЫ и ОСТАЮТСЯ на Sonnet: иврит теряет термины (приливы → «мерцания», самочувствие → «затраты»),
      корейский выдаёт несуществующее «폐경전증», японский — грамматические огрехи. Памятка дня uk: Sonnet $0.1242 против ru Haiku $0.0398. Учёт перевода дописан 2026-09-17 (endpoint `translate`) — после деплоя
      сверить реальную цену перевода вместо оценки $0.03.

- [ ] **Sonnet выдумывает имя клиента (ja):** разбор начинался с «Мサキさん» — кириллическая М + придуманное имя,
      хотя имени в данных нет. Личные имена запрещены ([[feedback_no_personal_names_in_ip]]) → нужен скраб или правило.
- [ ] **Иврит в синхронном режиме не доезжает:** Cloudflare 1102, при этом разбор оплачен ($0.1474 в `ai_usage`).
      В приложении спасает очередь; новые синхронные вызовы иврита не делать.

## 🧪 Пробный режим (решение 2026-09-17: пробник вместо 7 дней подписки)

- [x] **Код готов 2026-09-17 (не задеплоен, нужна сборка):** 3 пробных дня по уникальным дням прохода
      (`vialp_trial`), пейволл ждёт их исчерпания; `/analyze` с `trial:true` → `_TRIAL_FMT` (200–250 слов,
      2 темы базы, max_tokens 1600, ≈$0.03); ИИ-памятка в пробнике не заказывается — остаётся базовая;
      пометки на 12 языках: в тексте разбора (через `_renderAnalysis`), в памятке и в раскрытой главе.
- [x] **Покупка не ждёт конца пробника (2026-09-17):** пометка пробника кликабельна и открывает экран
      подписки (`_iapOpenFromProfile`); после покупки `_upgradeToday()` снимает пробные кэши дня и
      заказывает полный разбор и памятку, воркер пускает такой пересчёт один раз в сутки (`upgrade`);
      бывший подписчик пробник второй раз не получает (флаг `vialp_trial.sub`).
- [ ] **Проверить живьём после сборки:** три дня подряд (или подменой `vialp_trial`), что на 4-й день
      встаёт пейволл; что пробный разбор читается и укладывается в ≈$0.03 по `ai_usage` (note `trial`);
      что пометка видна и в печати; что покупка посреди пробного дня сразу даёт полный разбор. Пробник обнуляется переустановкой ($0.09) — закроется постоянным
      номером подписки.
- [x] **Решено 2026-09-17: пробника в сторах НЕ включаем.** Дни 1–3 — наше демо без карты, на 4-й день
      экран подписки и списание сразу. Минимальный бесплатный период у Apple и Google — 3 дня (одного дня
      не существует), то есть стороннее окно дало бы 6 бесплатных дней и $0.48 на человека вместо $0.09.
- [x] **Проверка кода 2026-09-17:** 15 тестов серверного лимита (готовое/pending/просроченное/сбой/`an:`/
      старый формат/памятка/upgrade ×2/EXPERT/новый день/без cid), 8 тестов пробника (день 1, повторная
      отметка, 3 дня, подписка, бывший подписчик, веб, битое хранилище), 6 тестов языка (uk→ru, перевод
      признаётся удачным). Синтаксис воркера и обеих страниц чист. Найдено и исправлено при проверке:
      пробный формат не получал строку языка; пробный день не засчитывался, если разбор приезжал после
      перезапуска (отметка перенесена в `_aiDailySave`/`_aiDailySaveDay`); урезание тем KB рвало связку
      «используй P-Fx» (убрано); флаг `upgrade` мог всплыть на следующий день (привязан ко дню).

## 💊 «Мои приёмы» — время приёма добавок и препаратов + напоминания (веб и воркер выкачены 2026-09-18, ждёт сборки)

- [x] **Код готов, 30 тестов пройдено.** Общий модуль `interpreter/intake.js` на обе страницы (против дрейфа клона).
      Список собирается из галочек «Добавки и препараты» + нового поля «Другие добавки» (`supp_other`) + «Другие
      препараты» (`meds_other`) + добавленного вручную в профиле. Время по правилам приёма (железо 07:00 натощак,
      щитовидная 06:30, D/омега 13:00 с жирной едой, магний 21:30, цинк 11:00), клиент меняет; даты «с/по» для
      курса/назначения; «напоминать». Глава памятки «Приём сегодня», раздел профиля «Мои приёмы», один раз после
      прохода — вопрос «Напоминать?». В ИИ-памятку уходит расписание клиента (`data.intake` → `_dayPlanSupps`).
      Напоминания: VIA-L — локальные (`notifyIntake` в notify-bridge.js, id ≥1000, курс → разовые до конца, ≤56);
      EXPERT — пустой пуш по cron (`/push/intake`, `runPushIntake`, D1 `push_intake`), SW различает приём/утро
      по часам в Cache. На заблокированном экране — только время. Кабинет: в протоколе «Во сколько / С / По /
      Напоминать», протокол доходит до EXPERT через `GET /expert/intake` с пометкой «от специалиста» (клиент
      не удаляет, может выключить напоминание).
- [x] **Доработка 2026-09-18 (47 тестов):** время КАЖДОГО препарата выбирается прямо в опроснике — блок «Когда вы их
      принимаете» на шаге добавок (`_ikStepRender`, поля `.manual-input` как у дат цикла/анализов), правки и
      отключение — в «Мой профиль» → «Мои приёмы» (сетка 2×2 тем же классом). VIA-L: приём в ту же минуту,
      что утреннее, — ОДНО уведомление «…· ваш список на 08:00» (как в EXPERT); смена времени/выключение
      утреннего пересобирает приём. Поймано тестом: проверка времени пропускала «25:99» — теперь диапазон.
- [x] **Выкачено 2026-09-18:** миграция (через `--command`: `--file` падает на OAuth «Authentication error 10000»),
      потом `wrangler deploy`; push (EXPERT-PWA и кабинет обновятся с сайта; SW EXPERT обновится сам);
      `app/sync-web.sh` копирует `intake.js` — новая сборка iOS/Android.
- [ ] **Проверить живьём:** VIA-L на телефоне — отметить железо и магний, вписать своё, включить напоминания,
      дождаться уведомления, проверить, что названий на экране блокировки нет; курс с датой «по» — после даты
      тишина. EXPERT-PWA на домашнем экране — пуш в начале часа приёма. Кабинет — протокол с временем
      появляется у клиента после перезапуска приложения.
- [ ] **Ограничения, о которых знаем:** EXPERT — точность до часа (cron ежечасный, приём 7:30 → пуш 7:00);
      VIA-L — курс с датой конца напоминает на 14 дней вперёд и продлевается при каждом открытии приложения.

## 🏋 «Что делать сегодня» по состоянию и вчерашней нагрузке (веб и воркер выкачены 2026-09-18, ждёт сборки)

Слабое место: «Движение вчера» не говорило, какие мышцы работали, воркер ответ только пересказывал,
а «сегодня» решалось одним флагом. Теперь в обоих файлах (VIA-L = нативка, EXPERT = PWA): при «Силовая»
подвопрос «Что нагружали» (`train_zone`), вопрос «Мышцы сегодня» (`sore`), подстановка с Oura
(`_movePrefill`), история 7 дней в `vialp_daily`, `move_ctx` в разбор (сигналы хуже личной нормы
считает клиент). Воркер `_todayMovePlan`: сначала состояние (2+ сигнала → восстановление, 1 или
лёгкая боль → лёгкий день), потом «вчера → сегодня нельзя», 3 дня подряд → отдых, план силовых
недели. Правила — `docs/FITNESS-SELECTION-RULES.md`.

- [x] **Выкачено 2026-09-18** (b08b3a7, воркер 73dbb74e) вместе с лимитом и «Моими приёмами»; нативка — `app/sync-web.sh` + сборка.
- [ ] **Проверить руками:** 12 языков новых вопросов; «Силовая» → появляется «Что нагружали», другой ответ → прячется;
      проход с Oura, где тренировка была вчера → ответ подставлен с подписью.
- [x] **Тренировки от Fitbit / Polar / Withings** (воркер, код 2026-09-18): общий `_workoutSummary` → те же поля, что у Oura.
      Права у всех трёх уже были — переподключать клиентов не нужно. Эндпоинты: Google Health `exercise`,
      Polar `/v3/exercises`, Withings `getworkouts`. ⚠️ Живьём не проверено — сверить форму ответа по первому логу.
      Ultrahuman: в API кольца тренировок нет.
- [x] **Apple Health / Health Connect — полный приём** (код 2026-09-18): + тренировки 7 дн, шаги за вчера (без
      двойного счёта телефон+часы), дыхание за сон, вес (→ профиль), давление за сутки (→ поля, если пусты).
      iOS: `healthkit-bridge.js` (+ `_vialWorkoutSummary` / `_vialHealthExtras`, общие для обоих мостов), текст
      разрешения в `Info.plist`. Android: `HealthConnectVialPlugin.kt` (новые типы через `safeRead` — отказ по
      одному типу больше не роняет весь импорт) + 4 разрешения в манифесте плагина. Шаги → разбор (`device.steps`).
      EXPERT PWA к Health доступа не имеет. % жира не берём — в разборе его некуда деть.
- [x] **Android v7 собрана 2026-09-18** (`app/store/build/vial-release-v7.aab`, в манифесте все 11 типов Health Connect) — загрузить в закрытый тест.
      ⚠️ Основная дорожка — «v4 - 2026-09-11» (список «VIA-L testers», 18). В «1.0 (1) — закрытый тест» до 18.09 не был
      отмечен список — v5 и v6 не дошли ни до кого; 18.09 владелец отметил тот же список. Все сборки — только в «v4».
      18.09: v7 в «v4» на проверке; в «1.0 (1)» v7 — черновик (не публиковать, не приостанавливать дорожку).
- [ ] **Владелец:** Play Console → «Разрешения Health Connect» — задекларировать READ_EXERCISE, READ_RESPIRATORY_RATE,
      (поля для новых типов Play Console покажет только ПОСЛЕ загрузки v7)
      READ_WEIGHT, READ_BLOOD_PRESSURE с обоснованием; сверить политику конфиденциальности (перечень данных).
- [ ] **Сборка и проверка:** push → `app/sync-web.sh` → iOS через Xcode Cloud/TestFlight, Android на JDK 17 в закрытый
      тест. На iPhone: окно Health спросит только новые типы. Проверить: тренировка вчера → «Движение вчера» отмечено;
      шаги не удвоены против приложения «Здоровье»; вес с весов попал в профиль.
- [ ] Android: у отказавшего от новых типов окно разрешений всплывает при каждом нажатии «Health Connect» (пока
      система сама не перестанет его показывать) — решить по живому тесту, нужно ли помнить «уже спрашивали».

## 💸 VIA-L + EXPERT — дневной лимит ИИ (веб и воркер выкачены 2026-09-18, ждёт сборки)

- [ ] **Выкатить:** воркер (`_dailyLimitHit` в `/analyze` и `/day-plan`: 1 успешный разбор + 1 план на cid в сутки)
      → `wrangler deploy` (tier pro/vio/elite/expert); VIA-L и EXPERT (смена языка не пересчитывает/не переводит, план дня из кэша на любом языке)
      → push (EXPERT PWA обновится с сайта) + `app/sync-web.sh` + новая сборка iOS и Android. Владелец копит правки под одну сборку.
- [x] **Защита пробника — 3 слоя в воркере (2026-09-17, 16 тестов пройдено):** счёт пробных дней НА СЕРВЕРЕ
      (`tr:<dev|cid>`, очистка данных приложения пробник не возвращает) + потолок 20 пробных разборов в сутки
      на IP (`tri:`) + ОБЩИЙ суточный потолок (`trg:`, по умолчанию 200 ≈ $6/день, меняется ключом
      `settings:trial_cap` в KV без деплоя) с одним письмом в TG при упоре. Клиент понимает `trial_over`
      (показывает подписку) и `trial_busy` (просит зайти позже) на 12 языках.
- [ ] **Следующая сборка (Android закрывается, iOS — нет):** добавить `@capacitor/device` и слать `dev`
      (Device.getId): на Android это SSAID и он переживает переустановку; на iOS это IDFV и он сбрасывается.
      Для iOS настоящий per-device флаг даёт только Apple DeviceCheck (нативный код + ключ Apple на сервере) —
      делать, если в `ai_usage`/`trg:` появятся живые следы накрутки.
- [ ] **Дневной лимит платящих: переустановка** (новый cid): слать постоянный номер подписки RevenueCat и считать лимит по нему.
      После выката — дописать журнал ARCHITECTURE §9 + interpreter/ARCHITECTURE.md.

---

## 🍏 VIA-L 1.0.1 (после ответа Apple на 2.1, решено 2026-09-11)

✅ **2026-09-15 1.0 (147) ОДОБРЕНА и выпущена** («Ready for Distribution») вместе с группой подписок и VIA-L Monthly; договоры/налоги/счёт Active, Small Business Program 15%.
- [ ] **1.0.1 = самая свежая сборка после пуша 07a807a (2026-09-13)**, не 149: в ней пульс Oura в разбор,
      «Сегодня начались месячные», «Размер текста», повтор разбора, подтягивание разбора с сервера.
      Перед Add Build сверить ловушку №1 (Deployment Preparation «TestFlight and App Store»).
- [ ] Что нового в 1.0.1 — короткий текст для стора (12 языков не обязательно, минимум en/uk/ru). Android: те же правки уехали в закрытый тест
как v5 (versionCode 5). Ждём отдельно: Ultrahuman прислал фикс 403 (проверено — 200, но данных нет:
к тестовому аккаунту не привязано кольцо), запрошены демо-данные; после них снять
`_nativeHideUltrahuman`. Следом:
- [x] ✅ 2026-09-12 **Памятка дня через фоновую очередь**, как разбор (`bg:true` → job → опрос). Сейчас `/day-plan` —
  обычный запрос до ~90 с: свёрнутое приложение его обрывает, клиент перезапрашивает — 4 платных
  вызова подряд 2026-09-11 23:17. Стартует только после разбора (`_anLoading`).
- [x] ✅ 2026-09-12 **«Меню готовится» вместо запасного меню**: пока `_bpAI` нет, `openBpChapter` молча рисует
  `_bpChapterData` — общие блюда без фото (выглядит как «модель выдумывает»; сама модель с
  `enforceDishCatalog` даёт 15/15 блюд каталога, проверено вживую).
- [x] ✅ 2026-09-12 **«Назад» с Terms/Privacy** ведёт туда, откуда пришли: `sync-web.sh` жёстко ставит
  `../index.html?tab=card`, с пейволла человек попадает в профиль.
- [x] ✅ 2026-09-12 **Android v5 в закрытом тесте** (трек «1.0 (1) — закрытый тест», versionCode 5,
  выложен скриптом: `python3 app/scripts/play-release.py upload --aab app/store/build/vial-release-v5.aab
  --track "1.0 (1) — закрытый тест" --yes`). Урок: v4 ушла в НОВЫЙ трек «v4 - 2026-09-11» и до
  тестировщиков не дошла — перед выкладкой всегда сверять треки командой `status`.

## 🎧 АУДИО: голос разбора и аудиогид тренировок (идея Марины и владельца, 2026-09-16)

Правило: **персональный текст — синтез на лету**, **общий текст — записан заранее** (один раз на все языки).
Провайдер — ElevenLabs (многоязычная модель $0,03 / 1000 знаков, клон голоса с 30 с). Модель фитнеса —
`docs/FITNESS-APP-MODEL.md`, текст-образец — `docs/FITNESS-AUDIO-SCRIPT-W1-RU.md` (демо m4a на рабочем столе).

- [ ] 🔑 **Ключ ElevenLabs** — владелец заводит аккаунт → `wrangler secret put ELEVENLABS_API_KEY`.
- [ ] **Кнопка «Прослушать разбор»** в дневном разборе (VIA-L и EXPERT, 12 языков):
      воркер `/tts` → ElevenLabs → mp3; **кэш на день** (R2/KV по клиенту и дню — повторное прослушивание бесплатно);
      генерация только по нажатию; ≈ $0,10 за первое прослушивание. Опция «голос устройства» — бесплатно, офлайн.
      ⚠️ Учёт расхода в `ai_usage`; разбор без служебных маркеров перед синтезом.
- [ ] **Пилот голоса:** тренировка №1 голосом из библиотеки ElevenLabs на ru + uk (< $1) → владелец слушает «человечность».
- [ ] **Аудиогид-конструктор** (после пилота): список аудиоблоков (вступления, ~45 упражнений: техника / ошибка /
      облегчённо / счёт, переходы, отдых, заминка) + правила сборки ИИ-тренировки дня (разминка → основная →
      заминка, вето по состояниям, «повторить вчерашнюю»). Блоки записываются заранее: ≈ $10 на 12 языков за голос.
      Личное вступление «сегодня так, потому что…» — синтез на лету. Видео остаётся 15 готовыми тренировками.
- [ ] Проигрыватель в приложении: части, фон и экран блокировки (нужен нативный плагин), скачивание для офлайна по Wi-Fi или сотовой сети (выбор + размер перед загрузкой),
      выбор языка и голоса в профиле; носитель прослушивает счёт/команды/стоп-сигналы на каждом языке.

## 💶 ОПЛАТА PWA КЛИЕНТОМ — остался юридический вопрос

Механика сделана и проверена на живом 2026-09-12 (журнал `ARCHITECTURE.md` §9): таблица
`client_payments`, ручки `/cabinet/client-payment(s)`, блок в карточке клиента, предупреждения
и подстановка числа месяцев по сроку выданного доступа.

**Как устроены деньги (уточнение владельца 2026-09-12):** клиенты **Марины** — канал
собственника сайта, €30/мес за PWA в итоге идут **Кириллу Селиванову** (Марина платит ему);
**внешние специалисты** сами принимают оплату от своих клиентов и платят нам.

- [ ] **Юрист.** В `legal/terms.html` Кирилл Селиванов прописан как поставщик (все языки), но
      платы €30/мес за приложение там **нет вообще**, а оплата описана через Hotmart как
      merchant of record — перевод на карту специалисту под это не подпадает. Нужны: строка о
      плате за пользование приложением (кто выставляет счёт в канале Марины и в канале внешнего
      специалиста), НДС, и сверка с `docs/OFFER-CLIENT-SPECIALIST-DRAFT.md`, где €30 записаны как
      услуга B2B предпринимателю. Показать вместе с текстом на лендинге (ключ `app_fee`).

## 🩺 ХВОСТЫ ПОСЛЕ ДНЯ 2026-09-12

- [ ] **Советник уходит от вопросов про еду** («це скоріше до дієтолога») — перестраховывается
      охранный фильтр. Посмотреть и смягчить: вопрос про ужин в велнес-рамке допустим.
- [ ] **Счётчик времени в блоке ожидания разбора** («Аналізую… 0:47») — владелец просил не делать
      сразу: анимация точек/силуэта гаснет при системном «Уменьшении движения», и понять, идёт ли
      работа, нельзя. Решение за владельцем.
- [ ] **Ultrahuman**: 403 починили (проверено — 200, данных нет: к аккаунту не привязано кольцо).
      ✅ 2026-09-17 демо-данные пришли, разбор в воркере исправлен (терял сон/глубокий/температуру/SpO2) и
      задеплоен. ✅ `_nativeHideUltrahuman` снят (`060174a`): Android v6 в закрытом тесте, iOS 1.0.2 —
      сборка Xcode Cloud после пуша, ⏳ прикрепить к новой версии 1.0.2 в ASC и подать. Затем ответить
      Sandeep (support@ultrahuman.com) с результатом.
- [x] ✅ 2026-09-17 Android v6 в закрытом тесте (`app/store/build/vial-release-v6.aab`, коммит `060174a`, Ultrahuman открыт).

## 🤖 ANDROID → GOOGLE PLAY (старт 2026-09-01)

План целиком — [`docs/ANDROID-PLAY-LAUNCH-PLAN.md`](../docs/ANDROID-PLAY-LAUNCH-PLAN.md).

**Закрыто:** ключ подписи и Play App Signing; targetSdk 36 + Play Billing 8 (`5b21978`);
сборка `vial-release-v2.aab` опубликована в закрытом тесте (v3 = `c000970`, 09-08); **v4 собрана
2026-09-11** (`app/store/build/vial-release-v4.aab`, versionCode 4, коммит `99bdd1c`: фоновый разбор
через очередь, «не сохранён» не показывается пока разбор в пути, ложные галочки 09-09, пейволл
«VIA-L Monthly», перевод описания специалиста, новая политика) — ⏳ загрузить в закрытый тест; витрина EN опубликована
(`9a69938`), локали uk/ru/es поданы (`477e764`, тексты — `docs/PLAY-LISTING-LOCALES.md`);
список из 15 тестировщиков заведён и применён к треку.

- 🔴 **12 живых тестировщиков × 14 дней подряд.** Срок стартует, когда установивших станет
  двенадцать ОДНОВРЕМЕННО, и обнуляется, если станет одиннадцать. Следить за счётчиком на
  панели управления; людям — письмо из `docs/TESTERS-INSTRUCTION-RU.md`.
  **2026-09-18: консоль «12 чел. на протяжении 10 дней» → 14 дней ≈ 2026-09-22** (счётчик «11» на
  главной — другая метрика, отстаёт; верить странице «Подача заявки на доступ к рабочей версии»).
- ✅ ~~Декларации «Контент приложения»~~ — **все 10 обработаны** (проверено по консоли
  2026-09-07): «Приложения для здоровья» 04.09 (7 разрешений Health Connect по
  `docs/PLAY-HEALTH-DECLARATION.md`), «Безопасность данных» 03.09, рекламный идентификатор,
  целевая аудитория, возрастные ограничения, учётные данные, финансовые функции,
  госучреждения, реклама, политика конфиденциальности. Вкладка «Требуются действия» пуста.
- ✅ ~~Довыкатить исследовательский синк~~ — **проверено 2026-09-07: уже в проде**. Схема
  применена целиком (`research_days`, `research_stats`, три индекса), данные идут (6 дней,
  fitbit+manual, ru). Сводок пока 0 по правилу малых ячеек: за август всего 3 дня записей,
  а `RESEARCH_STATS_MIN_N = 5`. Сводка за месяц считается 1-го числа в 9 UTC — первая
  реальная появится, когда в календарном месяце наберётся ≥5 наблюдений на ячейку.
- 🟡 **Политика приватности × 12 языков:** перечислить конкретные типы данных Health Connect
  (HRV, пульс покоя, пульс, сон, SpO₂, шаги, VO₂ max) и цель по каждому. **Ждёт слова владельца.**
- ✅ ~~**Google Play Billing**~~ — **закрыт 2026-09-12**: подписка `via_l_pro_monthly` в консоли,
  Android в RevenueCat с ключом в `iap-bridge.js`, флага `TEST_BYPASS_PAYWALL` давно нет (решает
  наличие ключа). Цены выровнены с App Store: еврозона 29,99 €, США 34,99 $, Канада 34,99 CAD,
  Швейцария 28,99 CHF, остальные пропорционально (`play-release.py prices`). Лицензионное
  тестирование включено — список «VIA-L testers» ОТМЕЧЕН галочкой (без неё Google берёт настоящую
  карту: живой случай 2026-09-12). Инструкция людям — `docs/TESTERS-PAYWALL-RU-UK.md`.
- 🟢 Заявка на доступ к рабочей версии — только после 14 дней теста; Google задаст вопросы
  о том, как тестировали и что нашли.
- 🟢 Локали de/fr/it/pl/he/ja/ko — по факту трафика из статистики Play, не заранее.

---

## ⚖️ ПЛАТФОРМА: правовой контур и защита клиента (план от 2026-08-03) ← ИДЁМ ПО ПОРЯДКУ

> Собрано из разговора с владельцем 2026-08-03. Модель: **специалисты — не работники VIA-L**,
> деньги клиента идут напрямую специалисту (платёжной интеграции у нас нет и не будет —
> это и защищает: платформа не сторона клиентского договора). Отсюда всё ниже.
> ⚠️ Юрист обязателен для текстов и для раздела «роли по данным» (контролёр/обработчик);
> форма (ФОП/ТОВ/ЕС) пока не создана. Паспортные данные НЕ храним — достаточно ФОП/налогового
> номера (открытый реестр) + адрес для корреспонденции.

### A. Правовой контур (защита от претензий)
- [x] **A1. ✅ Оговорка об ответственности в трёх местах:** оферта специалиста, письмо с кодом,
      экран «Мой наставник» в приложении. Текст: «Ведение оказывает [ФИО]; он несёт
      ответственность за консультации. VIA-L предоставляет программу и стороной договора
      не является». + прямо: деньги идут специалисту, платформа их не держит и не возвращает.
- [x] **A2. ✅ Акцепт оферты до оплаты + лог** (текст оферты — заглушка до юриста, версия `offer-v1`). Галочка «ознакомлен с условиями» рядом с кнопкой
      «Начать ведение»; лог: дата, время, версия текста оферты, id специалиста, устройство.
      Этот лог = доказательство (полноценная ЭЦП для €390–500 избыточна, режет конверсию).
- [x] **A3. ✅ Проверка специалиста ДО публикации.** Поля в карточке: публичные (образование,
      квалификация, номер регистрации/ФОП, сайт, город, языки) и служебные, видны только
      владельцу (юр. ФИО, налоговый номер, адрес для корреспонденции, телефон, дата и способ
      проверки, ссылка на подписанный договор). Галочка согласия на публикацию с датой.
      **Гейт:** не проверен → не публикуется в витрине и НЕ может выдавать доступы.
- [x] **A4. ✅ Бейдж «данные проверены платформой» + дата** на карточке в витрине.
- [x] **A5. ✅ Кнопка жалобы в один клик** из приложения клиента → владельцу (сигнал за минуту,
      а не через отзывы в интернете). Механика отключения уже есть.
- [x] **A6. ✅ Витрина: раскрытие подробностей по кнопке** (как просил
      владелец: клик открывает страницу, второй клик разворачивает детали).

### B. Непрерывность для клиента (специалист пропал)
- [x] **B1. ✅ Режим чтения после окончания срока.** Сейчас гейт закрывает интерфейс целиком:
      данные на устройстве целы, но недостижимы. Нужно: история и тренды доступны, новых
      разборов нет.
- [x] **B2. ✅ «Скачать мои данные» на экране «срок истёк»** (предупреждение за 7 дней — остаток) (право на переносимость) +
      предупреждение за 7 дней до конца прямо в приложении.
- [x] **B3. ✅ Передача клиента другому специалисту** — кнопка у владельца, срок сохраняется.
      Сейчас передать нечем, только правкой в базе.

### C. Дисциплина платежей (без вреда клиентам)
- [x] **C1. ✅ Ступенчатая блокировка вместо рубильника:** льготное окно 7–14 дней → запрет
      выдавать НОВЫЕ доступы (бьёт по источнику долга) → при глубокой просрочке режим чтения
      → `disabled` только вручную. Клиенты во всех случаях доигрывают оплаченный срок.
- [x] **C2. ✅ Причина отказа при входе.** Сейчас `lapsed` даёт «неверный пароль» — человек не
      понимает, что дело в оплате. Возвращать reason + сумму к оплате.
- [x] **C3. ✅ Квота-предохранитель (лимит неоплаченного софта, по умолчанию €100):** нельзя выдать доступов больше, чем оплачено/разрешено —
      ограничивает максимальный убыток при исчезновении специалиста (механизм квот есть).

### D. Гигиена доступа и доказательства
- [x] **D1. ✅ Кнопка «Сгенерировать» пароль** (16 знаков из crypto, алфавит без похожих символов,
      сразу в буфер, предупреждение «показать снова нельзя — в базе хэш»).
- [x] **D2. ✅ Смена пароля самим специалистом** — `/cabinet/my-password` + кнопка в шапке кабинета.
      Осталось на потом (не блокирует): приглашение по ссылке вместо передачи пароля.
- [x] **D3. Акт-выгрузка по клиенту:** когда доступ выдан, сколько дней, сколько разборов
      использовано, была ли переписка. Данные уже логируются — нужна выгрузка.

### E. Тексты (черновики мои, подписывает юрист)
- [x] **E1. Оферта клиент ↔ специалист** — черновик `docs/OFFER-CLIENT-SPECIALIST-DRAFT.md`
      (версия `offer-v1`, трёхсторонняя схема, мед-дисклеймер, возвраты, ограничение ответственности).
      Чужой шаблон (thebody.school) не подошёл — взят только скелет разделов. Подписывает юрист.
      Пересобран 2026-08-03 в рамочную оферту маркетплейса + Приложение №1; поля `country`/`legal_form`
      специалиста заведены и показаны на витрине.
- [x] **E2. Договор платформа ↔ специалист** — черновик `docs/AGREEMENT-PLATFORM-SPECIALIST-DRAFT.md`:
      лицензия на КАБИНЕТ + право выдавать EXPERT, €30/мес абонплата и €30/мес софт за клиента,
      стадии неплатежа, indemnity, «не работник платформы», клиентские доступы не отключаются,
      DPA-раздел (контролёр/обработчик + двойная роль). Подписывает юрист.
- [x] **E3. Политика данных** — черновик `docs/DATA-POLICY-CLIENT-DRAFT.md`: два режима хранения
      (VIA-L device-only vs EXPERT + карточка), роли включая joint controller по ИИ, сроки
      (здоровье 90 дней после ведения, факт услуги 3 года), права + кнопки в приложении, US-блок.
- [x] **E3-хвост ✅ (проверено 2026-08-07):** автоудаление 90 дней; политики разведены
      (`legal-app/privacy.html` — только устройство, EXPERT не упоминается; `legal/privacy.html` —
      ведение + раздел 7-бис); ссылки из обоих тарифов и под кнопкой удаления; переводы полные ×12.
      ⚠️ Для юриста: в EN-версии политики ведения 24 заголовка против 16 в остальных языках —
      EN детализирует подпункты (категории данных 3.1–3.4, права по GDPR/CCPA/APPI/PIPA отдельно),
      прочие языки сворачивают это в один раздел «Ваши права». Содержание не теряется, но
      паритета формулировок нет — обычно юристы просят выровнять.
- [x] **E4. Вопросы юристу** — `docs/LEGAL-QUESTIONS.md`: 20 вопросов пятью блоками + готовый
      блок «Контекст» для консультации. Юриста у владельца пока нет — список рабочий, не закрытый.
      Блокируют запуск: 1, 2, 4, 8, 10, 13, 14.

---

## 🔥 Ближайшее — хвосты сессии 2026-08-01/03



Контекст: журнал `ARCHITECTURE.md` §9 (записи 2026-08-01/03); память
[[project_supplements_module]], [[project_prompt_rules_placement]], [[project_token_economics]].
Сырьё OpenEvidence N-11 — `interpreter/Infa Cloude/E` (⚠️ НЕ в `Q`), третий круг —
`E/К. Вопросы третьего круга` (⚠️ ответ на вопрос 40 лежит в файле `49.txt`).

- [ ] **Живая проверка петли жалобы (2026-08-06/07).** В проде: вопрос «с чем вы пришли» в шаге
      «Что нового», дневное «лучше/так же/хуже», недельная оценка 0–10 + поле своими словами,
      траектория первым блоком в «Моём профиле», жалоба первым блоком в карточке кабинета, первая
      тема разбора и открытие недельного — от жалобы, эксперимент выбирается под неё. Посмотреть
      вживую: не забивает ли жалоба остальные темы; честно ли ведёт себя «медленная» жалоба
      (вес/либидо — дневной вопрос не задаётся); что придёт в недельном на 2-й оценке.
- [ ] **Живая проверка темы «Добавки» (2026-08-06).** Тема стала обязательной (`_suppThemeRule`),
      KB пополнен партией N-12 (еда/порции, критерий разрыва, мужская линия, креатин, сон, давление,
      белок). Посмотреть на следующем разборе ГЛАЗАМИ: не съедает ли она приоритет у главной темы,
      не всплывают ли дозы через `_stripDoses`, звучит ли «что закрывает еда» продуктами.
- [x] **✅ Блок анализов просмотрен владельцем 2026-08-24.** Сетка и свёрнутые группы на узком
      экране в порядке. Найдено и починено: (1) поле даты было прозрачным, без рамки, 10px цветом
      `--t3` — на тёмном фоне читалось как подпись «дата» без поля; теперь обычное поле, как значение;
      (2) два дисклеймера подряд дублировали друг друга («Диагноз по ним не ставится» + «Мы не
      интерпретируем анализы и не ставим по ним диагноз») и съедали пол-экрана до первого поля —
      повтор убран из первого абзаца во всех 12 языках, юридическая формулировка для App Review
      осталась во втором.
- [x] **✅ Замер по `ai_usage` сделан 2026-08-07** (14 дней, 135 вызовов, $4.73): кэш ЧИТАЕТСЯ
      (595k против 1.25M записанных на analyze) — `cache_control` оставляем; разбор триггеров
      guardrail был невозможен из-за вранья в `note` (indexOf вместо индекса совпадения) — починено.
- [x] **✅ Разбор триггеров guardrail сделан 2026-08-24** (свежие `note`, 22 дня): после починки
      сторож срабатывал на 6 разборах из 29 (21%), и ВСЕ свежие срабатывания ложные. Главный
      виновник — безусловное «подтвержда/свидетельств» (6 из 13 записей: «ВСР это подтверждает»,
      «возраст подтверждает этот этап») → загейтено объектом-диагнозом, как «означает».
      Побочно: self-check ни разу не дал PASS (6 из 6 → rewrite), то есть как фильтр он не работает —
      если ложные срабатывания не исчезнут, дешевле убрать проверку и оставить один rewrite.
      ⏳ Через 1–2 недели свежих данных посмотреть, сколько срабатываний осталось.
- [ ] **Первое закрытие петли эксперимента** у живого клиента — через 7 дней после первого
      недельного разбора; посмотреть, что человек реально увидит.
- [ ] **Глубокий сон в индексе — доделать честность до конца (обсудить).** Текст теперь
      оговаривает ненадёжность фаз, вопрос при синке снят, штраф в плитке смягчён. Осталось решить,
      не пора ли пересмотреть и вклад субъективного ответа о фазе в сам балл (сейчас в индекс он
      не входит вовсе — только в плитку).
- [ ] **Абонплата своему специалисту** (владелец): очистить `platform_since` у специалиста №1,
      если платформой его не облагаем — поле теперь есть в карточке.

---

- [ ] **Матрица «сигнал → пищевой паттерн» (ТЗ специалиста, переработано).** Канон —
      `docs/NUTRITION-MAPPING-SPEC.md` (2026-08-26): заменить универсальную тарелку триаж-логикой.
      Вход — НЕ диагнозы (App Store 1.4.1), а наши сигналы (жалоба `CMP_ITEMS`, `gi[]`, давление,
      WHtR, анализы, `meds[]`, фаза); арбитраж конфликтов и гейты безопасности — детерминированным
      кодом `selectDietPattern`, не инструкцией модели; ограничительные протоколы со сроком и
      реинтродукцией; модификаторы фильтруют фото-каталог блюд в «Памятке дня». Этапы 1–6 в §7.
      ✅ Выгрузка OpenEvidence закрыта 2026-08-26 (все 35 ответов в `interpreter/Infa Cloude/R/`,
      сжатие с выводами — `docs/NUTRITION-MAPPING-EVIDENCE.md`); каталог, арбитраж, гейты и блок
      движения переписаны по ней, 12 правок против черновика перечислены в §11. Список чипов
      утверждён владельцем. ⏳ Осталась реализация: этапы §7 (1 — каталог и `selectDietPattern`,
      2 — арбитраж и гейты с тестовыми кейсами, 3 — вживление в `/analyze`, 4 — фильтр фото-каталога
      блюд в `/dayplan`, 5 — недельный разбор, 6 — EXPERT) + блок состояний §9 и движение §10.
      ⚠️ Первым делом убрать из дневного плана обещание по льну и приливам (§11.1).
      Решение владельца 2026-08-26: добавить в опросник блок «состояния здоровья» чипами
      (base-каденс, необязательный, + «врач ограничил нагрузку» и «временное сейчас») — §9;
      состояния НЕ добавлять в whitelist исследовательского синка, дописать их в §3 текста
      согласия и в обе политики (12 языков). Движение под состояние и форму — §10.

- [ ] **Ежедневное уведомление — доделать в сборке.** Код готов (2026-08-26): мост, сборщик текста,
      фильтр экрана блокировки, секция в профиле, deep-link. Осталось `cd app && npm install &&
      npx cap sync` и проверка на симуляторе. ТЗ — `docs/DAILY-NOTIFICATION-SPEC.md`.
      Исходное описание:
      Одно в день, по умолчанию 08:00, текст — устойчивая часть «Памятки дня» (вода, разминка,
      завтрак), подстроенное под день не берём. ⚠️ Главное ограничение: текст виден на экране
      блокировки посторонним, поэтому ни одного слова о здоровье — только бытовое действие, всё
      остальное за нажатием. Не приходит, если проход за сегодня уже сделан. Разработка возможна
      на симуляторе (для TestFlight нужно платное членство Apple — оно есть).

## 📱 VIA-L — выпуск в App Store

**📋 Статус и остаток — [`docs/APP-STORE-LAUNCH-DAY.md`](../docs/APP-STORE-LAUNCH-DAY.md)
(переписан 2026-09-09).** Там таблица «что уже есть» со ссылками на коммиты, порядок настройки
RevenueCat, формы ASC и список проверок на устройстве.

**✅ Закрыто и НЕ является блокером** (сверено по репозиторию 2026-09-09 — прежние записи об этом
врали, потому что писались в конце августа):
членство **Apple Developer оплачено**; **запись приложения в App Store Connect существует**
(коммит `238d1b9` чинит «Prepare Build for App Store Connect failed» — билд доставлялся в неё);
**сборка Xcode Cloud дошла до TestFlight и стоит на iPhone владельца** (2026-09-03);
**`app/ios` под git** (23 файла, коммит `662a052`, настройки больше не теряются);
номер сборки подставляет `ci_pre_xcodebuild.sh`; юр-ссылки в приложении проверены вживую;
`legal-app/support.html` под Support URL готов. Витрина в App Store-сборке — решено 2026-08-25:
платный блок закрыт `_isWebSite()`, остаётся только бесплатное знакомство.

✅ **Сверено по коду 2026-09-14:** RevenueCat iOS настроен 2026-09-09 — в `iap-bridge.js` настоящий
ключ `appl_…`, пейволл на iOS включён; 1.0 подана и на ревью (см. блок «VIA-L 1.0.1» выше).
Прежние пункты «RevenueCat второе приложение / sandbox / сборка / рейтинг / метаданные» закрыты
подачей. ⚠️ Общий entitlement НЕ переносит покупку между сторами (SDK анонимный, `logIn()` нет) —
«Восстановить покупки» работает только внутри одного стора. Сборка: `cd app && ./sync-web.sh` →
Xcode Cloud; `pod install` — только при смене плагинов, с `LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8`.

Канон: [[project_app_iap_plan]], [[project_app_sim_build]], [[feedback_via_l_appstore_isolation]].
Обоснование именований для ревью — `docs/APP-REVIEW-LAB-NAMING-RATIONALE.md`.

- [ ] **Legal:** вычитка носителями 11 языков + хостинг `legal-app/` на чистом лендинге → URL в App Store Connect.
- [ ] **Долговременное хранение данных клиента — Уровень 1–2.** Сейчас всё в localStorage WebView (хрупко).
      ✅ Есть: бэкап export/import (Ур.3), cap метрик `*_daily` 1500 (≈4 г) / `*_ai_daily` 730 (≈2 г).
      🔴 Открыто: Ур.1 (нативный стор через Capacitor) + Ур.2 (iCloud/Google Drive + legal-согласие). [[project-storage-durability]]
- [ ] **Метки приватности — пересмотреть перед заполнением формы.** Два повода: 72-часовой кэш разбора
      (5e19a31) и opt-in исследовательский синк (b2151d9). «Data Not Collected» больше не подходит;
      честный ответ — Health & Fitness → App Functionality → **Not Linked to You** (аккаунта нет,
      идентификатор случайный). Сверить `PrivacyInfo.xcprivacy` перед сборкой. Обе политики уже
      описывают и кэш, и opt-in (f0218bf, 5590b1b, 12 языков).
- [ ] **Прогон на реальном устройстве:** карточка, авто-язык на iPhone, кэши день→день, RTL иврит,
      Capacitor-сборка, шифрование round-trip, советник (ответы по данным клиента + велнес-гардрейл).
- [ ] опц.: глубокий фолбэк «Памятки дня» на 12 языков (сейчас EN — App-Store-safe по решению владельца).
- [ ] **Фигура в разборе на мобильном.** Перешли на масштаб по высоте блока
  (`background-size:auto 100%`, привязка `top/bottom`). Правило `@media(max-width:820px)`
  (`width:38%;max-width:180px`) в вебе смотрится нормально — владелец проверит в нативке.

---

## 🧭 Воронка: VIA-L → витрина наставников → VIA-L EXPERT

Модель согласована с владельцем (2026-07-26/29). Канон — [[project_showcase_expert_funnel]],
`docs/PLATFORM-MODEL.md`. Кабинет специалиста и платформа как продукт описаны в
`docs/CABINET-MODEL.md` и `docs/PLATFORM-MODEL.md` — этапы 1–5 и локализация на 12 языков
закрыты и в проде, активных задач по ним нет.

- [ ] **Экономика — МОДЕЛЬ УТВЕРЖДЕНА 2026-07-29 (путь 1: доверие+счёт).** Потоки: клиент платит
      специалисту одну сумму (напр. €300 ведение 2 мес + €60 софт = €30/мес×2 = €360); специалист
      оставляет €300, нам переводит **на карту €60** (софт клиента) **+ €30/мес** за платформу.
      €30 — чистая абонплата за Кабинет, НЕ на токены. Токены покрывает €30/мес-софт, вшитый в цену
      специалиста. **Ограничители стоимости:** (а) EXPERT, как VIA-L, = **1 разбор/день/клиент** (клиент
      не может гонять бесчисленные проходы); (б) **квота разборов на каждый grant** (потолок нашей
      стоимости; механизм есть — `_planMax`/`expert_used`/`expert_max`). Себестоимость ≈€3/клиент/мес → €30
      даёт маржу ~€17. **Enforcement = доверие+счёт:** `/cabinet/expert-grant` при выдаче пишет сумму к оплате
      (срок×€30); Кабинет ведёт счёт специалиста «к оплате: €30 платформа + €N за выданные доступы» → платит
      на карту. Предоплаченный баланс — на потом, при росте. Никакой платёжной интеграции для клиентов
      (ни Apple IAP, ни веб-подписки) — специалист собирает всё сам.
- [ ] **Нативка VIA-L → синхро данных в Кабинет (будущая развилка, НЕ сейчас).** Compliance = функция кода,
      что шлётся в `/analyze`: нативка шлёт обычный код → велнес-разбор без мед-терминов (прошла модерацию).
      Можно добавить отправку СЫРЫХ данных в Кабинет (Apple не ревьюит серверную синхро к провайдеру), НО
      нативка обязана продолжать ПОКАЗЫВАТЬ пользователю только велнес; клинический разбор генерится/виден
      ВНЕ App Store (Кабинет спеца + EXPERT PWA). Больший объём + касание нативки → откладываем осознанно.
- [ ] **Оплата EXPERT: Hotmart → LiqPay (ПриватБанк).** Зафиксировано 2026-07-28.
      Разделение продуктов: **VIA-L** (глобал, App Store) остаётся на Apple IAP;
      **VIA-L EXPERT** продаётся с сайта нутрициолога (UA/RU + ЕС, разовый платёж
      за программу 8W/12W) → переводим на LiqPay. **Блокер — не код, а ФОП:** без
      украинского ФОП/ТОВ не выдадут merchant-аккаунт и ключи `public_key`/
      `private_key`. Пока сайт не афишируется — спешки нет, Hotmart стоит как есть.
      **Решение (разведка сделана):** цепочка выдачи уже разделена. Вход от
      Hotmart ([handleHotmartWebhook](../interpreter/cloudflare-worker.js#L969))
      только проверяет токен и разбирает *его* payload; всё полезное — в
      [handleInterpreterPurchase](../interpreter/cloudflare-worker.js#L1067):
      берёт FREE-код из Sheets (`assign_code`), TG-карточка, связка `код→клиент` в
      KV, карточка в кабинете, **письмо клиенту с кодом на 12 языках**
      ([buildInterpreterEmailBody](../interpreter/cloudflare-worker.js#L1148)) —
      ни строчки про Hotmart. **Переезд = дописать только «вход»:** новый роут
      `handleLiqpayCallback` (проверка подписи `base64(sha1(private_key+data+
      private_key))`, статус `success`, продукт из заказа) → вызвать ту же
      `handleInterpreterPurchase(...)`. **Коды уйдут автоматически, вручную слать
      не надо** (нужен лишь запас FREE-кодов `VIAL-EXPERT-8W/12W` в Sheets через
      `code-generator.html`). «Обрезать Hotmart» потом = удалить `/hotmart-webhook`,
      `handleHotmartWebhook`, `detectLangFromHotmart`; ядро не трогаем. Объём после
      получения ключей — ~полдня. См. memory [[project_tariff_merge_via_l_base]].
- [ ] **Экран «Мой наставник» без кода = тупик (VIA-L).** → покрыто пунктом «Витрина
      специалистов» выше (§ Воронка VIA-L → витрина → EXPERT PWA). Трёхслойность: 1) `?ref`
      → код подставлен; 2) ручной ввод; 3) кода нет → витрина. См. PLATFORM-MODEL §10 №3.
- [ ] **Класс B — внутренние id (опц., не срочно).** `MAX`/`tier:'pro'`/лоу-кейс
      `'elite'`/`'pro'` (флаги режима ИИ в /analyze) + ~40 ELITE-комментариев. НЕ ломает
      ничего; чистить только пачкой при желании нулевых вхождений. См. [[project_tariff_naming_canon]].

---

## 📊 Индекс дня, база знаний, новые модули

- [x] **Панель анализов расширена** (45 маркеров группами, HOMA-IR расчётом, сверки кодом,
      ориентиры в KB только для EXPERT, распознавание с фото в EXPERT) — `docs/LABS-PANEL-AUDIT.md`.
      Остаток: проверить распознавание на настоящем бланке лаборатории (тест был на синтетическом).


- [ ] **Живая проверка** текста разбора: без «норма для возраста»/«нехватка
      магния»; заметка «простужен» учитывается. (SpO₂-часть закрыта.)
- [ ] **Вычитка весов индекса профильным специалистом** → [`docs/INDEX-SCORING-REVIEW.md`](../docs/INDEX-SCORING-REVIEW.md):
      33 правила с весами и условиями + таблица весов §8. Нужен вердикт по каждому
      (ОК / изменить / убрать) и ответы на три вопроса: адекватна ли стартовая точка 70;
      нет ли перекоса (сон −28, гормоны −29); что пропущено — в т.ч. «второе дыхание» и
      «низкая температура + усталость». Ссылки проверены веб-поиском, выдуманных нет.
      MRS реализовывать нельзя (лицензия ZEG Berlin), ссылаться — можно; AMS свободна
      (`docs/INDEX-SCORING-SOURCES.md` §1.1). **После утверждения** можно показывать клиенту
      точную разбивку «−N» вместо нынешнего осторожного «Учтено в индексе».
- [ ] **Цикл-карта симптомов настроения (аналог DRSP).** Мы уже собираем и фазу цикла, и
      настроение/тревогу — можем строить карту симптомов по циклу и дескриптивно подсвечивать
      «симптомы весь цикл, а не только перед месячными → обсудите с врачом» (различение
      ПМДР / предменструальное обострение / первичное расстройство). ⚠️ **Стопор:** сейчас
      настроение/тревога/стресс идут НЕДЕЛЬНО/двухнедельно (канон ритма), а для настоящей
      DRSP нужна ДНЕВНАЯ привязка к фазе ≥2 циклов. Значит: либо повысить каденс сбора
      настроения (мин. дневная отметка с тегом фазы), либо ограничиться грубым недельным
      паттерном. Требует решения по каденсу + UI карты. Только дескриптивно, без диагноза.
- [x] **✅ Скрин дыхания во сне сделан 2026-08-24** (`_osaScreen` в воркере). Пункт частично
      устарел: поля храп / замеченные остановки / дневная сонливость (`sleep_breathing`) в опроснике
      УЖЕ были и питали паттерны P-F17/P-M9. Добавлен детерминированный скрин: факторы — храп,
      остановки, дневная сонливость, ночные подъёмы, ИМТ, талия, давление, возраст 50+, ночной SpO₂,
      постменопауза; порог у женщин 3, у мужчин 4 (STOP-BANG занижает женщин), замеченные остановки
      срабатывают сами по себе. Без «сонного» признака (храп/сонливость/остановки/SpO₂<93) молчит —
      иначе флаг ловил бы каждую вторую женщину 50+ с ноктурией. Формулировка: предложить обсудить
      с врачом ИССЛЕДОВАНИЕ СНА, без диагноза и без оценки вероятности. Каденс — раз в 14 дней
      (`_osaHintDue` → `osa_hint`). ⏳ Осталось посмотреть живой текст глазами.

---

## ⌚ Носимые устройства

Каркас единый (Worker `/<vendor>/start|callback|metrics`, KV `WEARABLE_TOKENS`,
фронт `connectWearable`/`fetchWearableLive`); детали — `interpreter/ARCHITECTURE.md` §15–16.
**Фактический прод (проверено живыми запросами 2026-08-04):** ✅ Oura, ✅ Withings (починен в тот
же день: секрет лежал со значением в ИМЕНИ, у ID был ведущий пробел), ✅ Polar (секреты заведены 2026-08-04),
Fitbit — только по `?vendors=1` (API закрывается 09.2026). Статус и заблокированные вендоры —
[[project_wearable_integrations_status]], стратегия сбора — [[project_import_panel_modes]].

- [ ] **⚠️ Withings: перевыпустить client_secret, когда будет удобно.** Действующее значение
      светилось в имени секрета (видно всем с доступом к Cloudflare и в логах). Не срочно, но лучше
      сменить в кабинете Withings → `wrangler secret put WITHINGS_CLIENT_SECRET`.
- [x] **✅ Oura и Fitbit подтверждены владельцем вживую (2026-08-07)** — данные приходят, у Fitbit
      попутно чинён выбор ночи и подсчёт сна по фазам (сходится с приложением трекера).
- [ ] **Polar и Withings — устройств у владельца НЕТ, вживую не проверить.** Сделан аудит кода
      вместо прогона: выбор ночи у обоих по полю `date` самой записи (ошибки Fitbit с createTime
      здесь нет); у Withings `total_sleep_time` по докам = время сна без бодрствования — верно;
      у Polar порядок перевёрнут на безопасный (сумма фаз первым, готовое поле фолбэком).
      Останется проверить, когда появится устройство у клиента или на руках.

- [ ] **📨 Запросы на affiliate-скидку для клиентов — Oura + Ultrahuman (✅ ОТПРАВЛЕНЫ 2026-07-29, ждём ответа).**
      Слал с `integration@via-l.com`. **Oura** → форма «Oura for Organizations» (объём «5-19», Health & Wellness,
      цель = скидка клиентам + мягко API); API-трек у Oura отдельный (партнёрство >10 отклонено, Becky #6994484).
      **Ultrahuman** → partnership-форма: (1) партнёрский код скидки клиентам, (2) API-доступ (`ring_data`/`cgm_data`/
      `profile`). ⚠️ **affiliate ≠ API-коды** — разные команды. Тексты (EN+RU) — в диалоге 2026-07-29. ⏳ Дождаться
      ответов; если скидки идут через отдельную affiliate-программу — попросить переключить туда.
      **📨 2026-08-25 — вторая волна, отправлена личным людям (общие адреса молчат).** Только реферальная
      ссылка + код-скидка, **про API намеренно ни слова**. Oura → Becky, ответом в тред тикета #6994484
      (текст: `docs/OURA-AFFILIATE-LETTER.md`). Ultrahuman → Vighnesh, `partners@ultrahuman.com`, ответом
      в его июньский тред (текст: `docs/ULTRAHUMAN-AFFILIATE-LETTER.md`). Оба письма EN + RU-перевод.
      **Oura — 🔴 ОТКАЗ 2026-08-26** (тикет #7805684, ответил Dante, Member Success Support, снова не
      партнёрский отдел): реферальной программы для объёмов **меньше 20 колец** у них нет — ни ссылки,
      ни атрибуции, ни бизнес-кода. Покупка только через обычный магазин Oura. Возвращаться к разговору
      имеет смысл на объёме 20+ покупателей; ветка «Oura for Organizations» («5-19») закрыта. На OAuth-
      интеграцию Oura отказ не влияет. Разбор — в `docs/OURA-AFFILIATE-LETTER.md`.
      **🟢 Oura, второй ответ 2026-08-26 (тикет #7814075) — API-дверь ОТКРЫТА, отказ Becky устарел.**
      Production-доступ >10 пользователей подаётся обычным путём через `cloud.ouraring.com/docs`,
      партнёрство НЕ нужно. Требования: имя+описание приложения, homepage, ToS, Privacy Policy,
      рабочие redirect URL, **минимум один подключённый пользователь**; ответ до 2 недель. Всё это
      у нас есть (via-l.com, `legal/terms.html`+`legal/privacy.html`, redirect
      `https://interpreter.viaelcom.workers.dev/oura/callback`, кольцо владельца подключено).
      **🎉 ОДОБРЕНО 2026-08-26 (тикет #7815321, Helder) — ЛИМИТ 10 ПОЛЬЗОВАТЕЛЕЙ СНЯТ.** Ответ
      пришёл через ~45 минут после подачи. В коде менять нечего: Client ID/Secret те же, роуты
      `/oura/*` работают, исчез только потолок. ⚠️ Условия: API Agreement (нарушение = отзыв
      доступа без предупреждения); имя/логотипы Oura в брендинге нашего приложения запрещены
      (у нас чисто: имя «VIA·L», «Oura» только как название источника, в репо фото кольца, не
      логотипы). ⏳ Хвост: сверить с **Oura Branding Guidelines** формулировку атрибуции рядом
      с цифрами и право показывать фото кольца в ленте совместимости.
      Было: **✅ ПОДАНО 2026-08-26 — статус «In Review».** Приложение уже было
      заведено и заполнено; подача = «Request more users» → «Submit». ⚠️ Портал переехал на
      **`developer.ouraring.com`** (старый `cloud.ouraring.com` с 15.10.2025 только редактирует).
      Client Id `65bcca60-12ef-467e-a52a-7cd600021344`. Перед подачей в политику приватности ×12
      языков добавлен пункт про подключённые аккаунты устройств и Oura API (задеплоено). По опту: цены поддержка не даёт, заявка через **Oura For
      Business**, и кольца можно слать **сразу конечным людям**, не в одно место. Разбор —
      `docs/OURA-FOLLOWUP-LETTER.md`.
      **🔴 Сверка с API Agreement (2026-08-26, `docs/OURA-COMPLIANCE-REVIEW.md`) нашла конфликт:**
      `research_days` («Помочь улучшить приложение») принимает hrv/rhr/sleep/spo2/tempDev/vo2/readiness,
      а §6(g) запрещает использовать User Data для улучшения любой AI-системы **независимо от согласия
      пользователя**. Правка: на сервере отбрасывать метрики трекера при `src=oura` (самооценки — наши,
      их не касается). Не внесена — ждёт решения владельца. 🟡 Второй пункт: фото кольца в ленте
      совместимости — спросить `branduse@ouraring.com` либо заменить на нейтральную иконку.
      Брендинг в остальном чист: логотипы не используем, «Oura» только как источник, обязательной
      формулировки атрибуции у них нет вовсе.
      ⏳ **Ultrahuman API — ✅ ключи получены 2026-09-10 самостоятельно** в кабинете `vision.ultrahuman.com/developer`
      (заявки в partnerships не понадобились), роуты `/ultrahuman/*` + кнопка «Подключить» написаны.
      Задеплоено; вход и токен проверены вживую, но метрики отдают **403** (аккаунт без кольца или доступ не включён).
      📨 10.09 письмо в `support@ultrahuman.com` (`docs/ULTRAHUMAN-API-403-LETTER.md`) — ⏳ ждём ответа.
      Осталось: сверить ключи ответа на первом кольце (`wrangler tail`), прочитать их API Agreement и решить
      про `WEARABLE_RESEARCH_BLOCK`. Детали — `interpreter/wearables/ultrahuman.md`.
      Affiliate-письмо Vighnesh (25.08) — без ответа. Если ответят согласием — ссылка и код идут в онбординг-материалы
      и внутрь продукта; у Oura до порога — обычная ссылка на магазин, скидку пользователю не обещаем.

---

## ⚖️ Юридическая упаковка (EU MDR / FTC / Hotmart)

> Рамка регуляторов: **FTC** — health claims; **FDA** — SaMD; **EU MDR 2017/745** —
> «диагностирует/предотвращает/лечит» переводит продукт в медизделие; Hotmart банит
> «альтернативу медпомощи». Копипаст-тексты дисклеймеров и таблица замен —
> `docs/HOTMART-COMPLIANCE-BRIEF.md`.
>
> **Аудит лендингов 2026-08-03:** заголовков-обещаний нет ни на одной из пяти страниц
> (проверено по словам лечение/терапия/диагноз/гарантируем и по глаголам
> уберём/устраним/вернём/нормализуем — ноль совпадений). Футер-дисклеймер был зашит
> только по-английски на четырёх страницах программ — локализован на 4 языка.

- [ ] **Проверить страницу оплаты Hotmart и письма** — единственное место, где дисклеймер ещё не
      проверялся глазами. На самих лендингах он теперь есть везде и на языке читателя.
- [ ] **Статус авторов — «наставник», не «врач».** Пройти по био и подписям на лендингах и в
      витрине специалистов: позиционирование health-coach / нутрициолог / консультант по образу
      жизни. (Проверялось частично; отдельного аудита формулировок не было.)
- [ ] **Guardrail на `/day-plan` — РЕШЕНО 2026-08-24: не вешаем, ждём пробу.** Две причины: (1) он
      переписывает СВОБОДНЫЙ текст, а памятка — строгий JSON по схеме, перегенерация ломала бы
      структуру; (2) замер показал 21% срабатываний на `/analyze`, все ложные. Вместо платного
      self-check поставлена БЕСПЛАТНАЯ проба тем же детектором: строки плана прогоняются через
      `_riskScan`, и при совпадении пишется строка в `ai_usage` с `endpoint='dayplan-risk'` и
      нулевой стоимостью. ⏳ Через 1–2 недели посмотреть: пусто → guardrail не нужен вовсе;
      есть срабатывания → чинить детерминированно (фильтр под конкретный случай), а не LLM-ом.

---

## 🔒 Канон ИИ-воркера — анти-регресс (не задача, а правило)

- **Заморожен compliance-фундамент** (не ослаблять без слова владельца): `temperature` 0.35,
  `AI_SAFETY_RULES`, детерминированные фильтры (`_stripHerbLines`, `_stripAnyDoses`,
  `_stripCodeTokens`, `wellnessHardScrub`).
- **Приоритет у детерминированного слоя, не у «естественности» текста.** Каждое правило
  стоит на конкретном дефекте; зелёный автоскан ≠ «всё ок» — периодически смотреть живой
  вывод ГЛАЗАМИ.
- **НО фильтр обязан ловить УТВЕРЖДЕНИЕ, а не слово** (уроки 2026-08-02/03): регекс на
  «диагноз» срабатывал на нашем же дисклеймере, на «болезнь» — в отрицании, на «означает» —
  в связке метрик клиента. Каузальные и медицинские маркеры гейтить объектом-диагнозом и
  проверять на отрицание. ⚠️ `\b` в JS не видит кириллицу — границы юникодные.
- **Де-медикализация не должна прятать данные, на которых стоит безопасность:** название
  анализа, органа и класса лекарства, о котором человек сообщил сам, — называем; прячем
  ДИАГНОЗ. Пословные подмены, которые нельзя согласовать по падежам, ретируем
  (так снята «воспаление» → «фоновая нагрузка»).
- **Само-триаж дефектов:** дезинформация = БЛОКЕР, чинить сразу; косметика — в очередь.
- Каноны терминологии и claim'ов — [[project_prompt_rules_placement]],
  [[feedback_via_l_appstore_isolation]].

---

## Правила работы (важно)

- Одно конкретное предложение прозой, без опросников. Короткие ответы.
- **Approval per-file** — не переносить паттерн с тарифа на тариф без «ок».
- **«Аккуратненько»:** синтаксис-чек inline-JS ДО коммита; при правке KB — сверять размеры
  блоков до и после (заменять содержимое можно только там, где это заглушка).
- **Деплой:** HTML → GitHub Pages (`git push`, домен **via-l.com**); воркер →
  `cd interpreter && wrangler deploy`. Секреты — `wrangler secret put` (репо публичный).
  ⚠️ Первый запрос сразу после деплоя может прийти на старую версию — проверять повтором.
- Папки `Infa Cloude/*` — провенанс (gitignore), не коммитить.
