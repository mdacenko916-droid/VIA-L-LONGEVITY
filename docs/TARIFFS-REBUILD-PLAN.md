# Перестройка тарифов ИП — план и цель

Статус на 2026-06-19. Этот документ — точка входа для продолжения работы по выравниванию
тарифов ИП под новый **PRO**. Связано: `interpreter/interpreter-*.html`,
`docs/TARIFF-DATA-SOURCE-MODEL.md`, память `project-tariff-design-divergence`,
`project-pro-funnel-structure`.

## К чему идём (конечная цель)

**КАНОН (владелец, 2026-06-19): EXPERT/ELITE = тот же PRO + слой специалиста.** Приём и анализ
данных в EXPERT/ELITE должны быть **точь-в-точь как в PRO** (гаджет-панель, каденс, когниция,
приливы, оценка движком). Слой специалиста — это только НАДСТРОЙКА: онбординг-анкета, счётчик
разборов, лаб-разбор, чат, PDF, ЗГТ. Порт идёт в одну сторону: **PRO → expert, PRO → elite**.
**VIO — исключение:** «PRO без гаджета» (ручной ввод 8 показателей вместо гаджет-панели).

Все 4 инструмента ИП — **в едином стиле и структуре нового PRO**, но каждый со своим
**цветом-акцентом** и своей **тарифной спецификой**. Эталон структуры/UX — `interpreter-via-l.html`
(пересобран 2026-06-18: каденс блоков, заголовки-группы, редизайн приливов/когниции/ПМС/
тревожности, гаджет-панель, заголовки, мобайл, золото на тёмном).

## Файлы и идентичность тарифов

4 отдельных HTML (~950КБ; vio ~667КБ) в `interpreter/`. Это **не общий файл** — копии,
исторически сильно разошедшиеся (авто-патч pro→тариф не ложится: 11/54 хунка). У каждого свой
`<title>`, `DEV_CODES`, цены/CTA. Гейт фич — `vial_access==='MAX'` (одинаков в pro/expert).

| Тариф | Файл | Цвет-акцент | Палитра (из `index.html`) | Особенность |
|---|---|---|---|---|
| **PRO** | `interpreter-via-l.html` | золото | `#1a1408`/`#e2b96a` | эталон; гаджет-панель; каденс |
| **EXPERT** (Pro+Expert €79) | `interpreter-pro-expert.html` | синий | `#10141e`/`#7898ff` (`.plan-expert`) | **онбординг-анкета** (`onb_*`/`submitOnboarding`) + **счётчик разборов** (`expertState`/`renderExpertCounter`); код `VIAL-EXPERT-2024`/`VIAL-EXPERT-ONB-2024`→MAX; БЕЗ гаджет-панели |
| **ELITE** (€390/€590) | `interpreter-elite.html` | золото | как PRO | как Expert + лаб-разбор/чат/PDF/ЗГТ; больше CTA; БЕЗ гаджет-панели |
| **VIO** (€9.90, без гаджета) | `interpreter-vio.html` | фиолет | `#120d1e`/`#b48af0` (`.plan-free`) | **ручной ввод**, нет гаджет-панели, нет ползунка тревожности |

## Метод (как переносить, по уму)

**Дизайн (безопасно, сделано):** аддитивный CSS-override в конце `<style>` в цвет тарифа
(карточки `.step>.card` + border + glow + halo off, `.card-accent/::before`, бейдж/деск, чипы,
`.manual-input`, `.topbar-home`, модал `.gate-*`). Палитры — из тарифных карточек `index.html`.

**Чистый структурный порт (блок == pro-old по структуре + движок совместим):**
рецепт — добавить i18n-мердж-блок после строки `['ru'..].forEach(T[l]=Object.assign({},T.en,T[l]))`,
заменить разметку шага, поправить data-build. ОБЯЗАТЕЛЬНО: синтаксис-чек inline-JS + проверка,
что движок (ветки оценки) читает те же значения.

**Нечистый порт (блок структурно ДРУГОЙ в тарифе) — НЕ форсить наскоком:** требует переписать
движок живой платной страницы = риск. Только осознанным продуктовым решением + тест.

## Статус по блокам (✅ сделано / ⬜ осталось / 🚫 не лёг чисто)

**Дизайн/цвет:** PRO ✅ · EXPERT ✅ · ELITE ✅ · VIO ✅

**Структура:**
- ПМС → 11-пунктовый чеклист (движок выводит тяжесть из числа: 0/1–3/4–6/7+ → none/mild/mod/severe):
  EXPERT ✅ · ELITE ✅ · VIO ✅
- Тревожность → 4 уровня (ползунок 1–10 → 4 радио value 1/4/7/9, getRadio): EXPERT ✅ · ELITE ✅ · VIO 🚫(нет ползунка)
- 🚫 **Когниция** — в тарифах память категориальная (good/slight/poor/severe) + туман 4-уровневый
  (none/morning/daily/constant), движок читает их; в pro — память 1–10 + туман Да/Нет. Унификация =
  переписать `_mapMem`/`_mapFog` и ветки оценки тарифа. **Продуктовое решение.**
- 🚫 **Приливы** — в тарифах доп-поля `hotfreq`/`hf_time` + температура вводится вручную (нет
  гаджет-панели); pro-редизайн «за сутки» со скрытой температурой не ложится. **Продуктовое решение.**
- 🚫 **Самочувствие / Настроение / Раздражительность** — движок тарифа читает symptoms/mood/
  irritability; в pro удалены. Удалять в тарифах = трогать оценку.
- ⬜ **Каденс / заголовки-группы / гаджет-панель / кнопка результата инлайн / мобайл-фиксы** —
  pro-специфика для гаджетного дневного потока; у тарифов другой flow (онбординг). Переносить
  выборочно и осознанно, НЕ механически.

## Открытые задачи (для нового чата)

**Решение по «нечистым» блокам ПРИНЯТО (2026-06-19): унифицируем.** EXPERT/ELITE = PRO + специалист,
поэтому когниция, приливы, каденс, гаджет-панель — **как в PRO**, без исключений. Порт PRO → expert,
PRO → elite, поблочно, на живых платных страницах → бэкап + синтаксис-чек + тест владельцем перед
переходом к следующему файлу. Порядок: **expert первым (€79, меньше риск), затем elite (€390/590).**

Что занести в expert/elite из PRO:
1. **Гаджет-панель** (приём данных) — сейчас у них её НЕТ.
2. **Когниция** PRO-версии (память 1–10, туман Да/Нет) — переписать `_mapMem`/`_mapFog` + ветки оценки.
3. **Приливы** PRO-версии («за сутки», температура из гаджет-панели) — теперь ложится, панель приедет.
4. **Каденс / заголовки-группы / инлайн-кнопка результата / мобайл-фиксы.**
5. Убрать `symptoms/mood/irritability` (в PRO удалены) — синхронно с ветками оценки.
Сохранить поверх: онбординг (`onb_*`/`submitOnboarding`), счётчик (`expertState`/`renderExpertCounter`),
лаб-разбор/чат/PDF/ЗГТ (elite), цвет (expert синий / elite золото).

**VIO — отдельная задача (исключение):** собрать панель ручного ввода 8 показателей (HRV/пульс/сон/
глубокий/температура/SpO₂/VO₂/готовность) вместо гаджет-панели. См. `project-pro-vio-redesign`.

## Правила (из tasks/TODO.md)
- Approval per-file, не переносить паттерн без «ок».
- Бэкап + синтаксис-чек inline-JS ДО коммита; правка pro → чек → перенос.
- Деплой: HTML → `git push` (via-l.com); воркер → `cd interpreter && wrangler deploy`.
- Это **живые платные страницы** (€9.90/€79/€390/€590) — не ломать; рискованное только осознанно.
