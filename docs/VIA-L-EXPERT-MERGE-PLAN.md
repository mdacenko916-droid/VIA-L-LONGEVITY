# Слияние EXPERT + ELITE → один тариф «VIA-L EXPERT» — план и карта удаления

Статус: старт 2026-07-25 (владелец). Заменяет устаревший [`TARIFFS-REBUILD-PLAN.md`](TARIFFS-REBUILD-PLAN.md)
(там 4-тарифная модель с портом PRO-структуры в отдельные страницы — эта идея её упрощает).
Связано: [[project_tariff_merge_via_l_base]], [[project_ip_weekly_report]], [[project_expert_bot_reply_flow]],
[[project_cabinet_redesign]], [[project_vedenie_channel_target]].

## Цель (канон владельца 2026-07-25)

**Один платный клинический тариф «VIA-L EXPERT»:**
- **Архитектура = КЛОН via-l (решение владельца 2026-07-25).** НЕ втискиваем via-l-проход в elite (у elite
  свой проход/движок → каша и риск). Вместо этого: **`interpreter-via-l-expert.html` = клон `interpreter-via-l.html`**
  + донорский специалистский слой из ELITE. Оригинал `interpreter-via-l.html` остаётся чистым бесплатным
  велнесом (App Store), НЕ трогаем. И `pro-expert.html`, и `elite.html` в итоге удаляются.
- **Мозги — в воркере (общие):** KB/паттерны/детектор/промпты не дублируются, клон наследует их автоматически.
  Дублируется только HTML-проход (форма опросника) → правки формы вносить в оба файла.
- **Проход (UX/анкета/интерпретатор)** = via-l-проход, КАК ЕСТЬ (в клоне).
- **Две цены = из ELITE:** €390 / 8 недель (`ELITE-8W`, Hotmart `W106049757Q`) и €590 / 12 недель
  (`ELITE-12W`, Hotmart `A106049851J`).
- **Мед-ограничения СНЯТЫ** — под кодом VIA-L EXPERT проход идёт по клиническому промпту (не велнес-гейт).
- **EXPERT (€79 «PRO+EXPERT») — УДАЛЯЕМ ПОЛНОСТЬЮ** (файл, продукт, роутинг, карточки, ссылки, коды).
  Правило владельца: **удаляем, а не прячем/комментируем.**
- Продаётся на Hotmart, крепится к сайту via-l нутрициолог.

## ⚠️ «EXPERT» в коде — ДВА разных смысла (не перепутать при удалении!)

1. **Продукт/страница «PRO+EXPERT €79»** → **УДАЛИТЬ.** (product 7838826, `interpreter-pro-expert.html`,
   ссылка `K106049657S`, карточка в index, коды VIAL-EXPERT, apps-script EXPERT-подписка.)
2. **«EXPERT/ELITE» как КЛИНИЧЕСКИЙ РЕЖИМ** (SYSTEM_PROMPT, клин. язык, юр-рамка, кабинет, недельный
   отчёт в карточку) → **ОСТАВИТЬ.** Именно им работает новый VIA-L EXPERT. Комментарии/ветки
   «EXPERT/ELITE» в воркере, описывающие клинику, НЕ трогать (они теперь про VIA-L EXPERT).

## Именование

Видимое имя — **«VIA-L EXPERT»**. Внутренние идентификаторы тарифа (`ELITE-8W`/`ELITE-12W`) и
Hotmart product-ID (7838876/7838925) **оставляем** — не трогаем платёжку/KV, меняем только
отображаемое имя (`TIER_NAMES`, письма, UI).

---

## ЭТАП 1 — удаление EXPERT + переименование + роутинг (видимое, низкий риск)

**СТАТУС: сделано и в проде 2026-07-25** (кроме apps-script + текста описания лендинга — см. хвосты внизу).

Карта правок (полный свод grep 2026-07-25):

**Удалить файл:**
- [x] `interpreter/interpreter-pro-expert.html` (весь, ~1 МБ). ✅

**`interpreter/cloudflare-worker.js`:**
- [x] product-map `7838826: EXPERT €79/мес` → удалено. ✅
- [x] `TIER_NAMES`: убран `EXPERT`, `ELITE-8W`→`VIA-L EXPERT · 8 нед`, `ELITE-12W`→`VIA-L EXPERT · 12 нед`. ✅
- [x] `ipLinkFor` — ветка `EXPERT → pro-expert.html` удалена. ✅ ELITE-ветка пока на elite.html (→ клон в Этапе 2).

**`interpreter/index.html`:**
- [x] `.plan-expert` CSS → удалено. ✅
- [x] Карточка «PRO + EXPERT €79» → удалена. ✅
- [x] `HOTMART_LINKS.pro_expert` (`K106049657S`), `contactExpert()` → удалены. ✅
- [x] Elite-карточка переименована в «VIA-L EXPERT» (заголовок). Elite-ссылки целы: 8W `W106049757Q`, 12W `A106049851J`. ✅
- [ ] ⏳ ХВОСТ (позже, по владельцу): текст описания лендинга — подзаголовок «Два рівні живого супроводу»
      + описания карточки; осиротевшие i18n-ключи `plan_exp_*` (×12) вычистить в том же проходе.

**`interpreter/apps-script.js` (Google Apps Script, генерация/валидация кодов):**
- [ ] EXPERT-подписка: `SUBSCRIPTION_DAYS`/`EXPERT_MAX`/`EXPERT_WIN_MS`/`EXPERT_COOL_MS` (L27-30) —
      оценить: ELITE использует свой счётчик (8/12 разборов), EXPERT (2/окно) уходит. Убрать EXPERT-ветку.
- [ ] `DEV_CODES` (L46): убрать `VIAL-EXPERT-2024`, `VIAL-EXPERT-ONB-2024`.
- [ ] Ветки онбординга/активации «PRO+EXPERT» (L180, L253 и др.) — оставить только ELITE.
      ⚠️ Это серверный код (деплоится отдельно в Apps Script, не через git push) — синхронизировать вручную.

**Прочее:**
- [ ] `sitemap.xml` / любые ссылки на pro-expert — проверить.

## ЭТАП 2 — создать клон via-l-expert + перенести в него весь слой ELITE (деликатное)

Ядро того, на что владелец просил обратить внимание. **Сначала клонируем**, потом переносим слой:

- [ ] **Клон.** `cp interpreter-via-l.html interpreter-via-l-expert.html`; сменить `<title>`, tier-маркер
      (шлёт в воркер `VIA-L EXPERT`/ELITE-код), при желании акцент-цвет. Проход остаётся идентичным.
- [ ] **Роутинг.** `ipLinkFor`: ELITE-8W/12W → `interpreter-via-l-expert.html` (не elite.html, не via-l.html).

Дальше — перенести из ELITE в клон (проверять, что срабатывает под кодом VIA-L EXPERT = ELITE-8W/12W):

- [ ] **Клинический режим.** Клон под кодом → клинический промпт (SYSTEM_PROMPT), мед-ограничения сняты
      (tier-switch в воркере по коду). Оригинал via-l остаётся велнесом.
- [ ] **Недельный отчёт → нутрициологу.** В via-l недельный разбор сейчас клиентский; при VIA-L EXPERT
      он должен уходить В КАРТОЧКУ кабинета (`breakdowns[type:weekly]`, `handleWeeklyReport`), как у elite.
      Проверить, что via-l шлёт code + tier так, чтобы воркер положил разбор в карточку. [[project_ip_weekly_report]]
- [ ] **Анкета / онбординг.** `onb_*/submitOnboarding` + `book/anketa` (доставка по коду из письма).
      Подключить онбординг-анкету к via-l-проходу (сейчас она в elite/pro-expert). [[project_vedenie_channel_target]]
- [ ] **Ответ/PDF/чат специалиста.** Reply-flow нутрициолога (KV draft/editing, PDF) + недельный PDF ELITE
      + TG-чат — навесить на via-l под кодом. [[project_expert_bot_reply_flow]]
- [ ] **Cal.com / Zoom.** Видео-консультация ELITE (ru/uk) — привязать к VIA-L EXPERT. [[project_booking_system]]
- [ ] **Кабинет-ингест.** Карточка при оплате уже создаётся для не-PRO (`cabinetUpsertFromPayment`);
      убедиться, что via-l-данные под кодом падают в неё (автоингест). [[project_cabinet_redesign]]
- [ ] **Гейт/коды в via-l.** Добавить в via-l распознавание кодов ELITE-8W/12W (сейчас DEV_CODES ELITE
      живут в `interpreter-elite.html`), счётчик разборов (8/12).

## ЭТАП 3 — удалить `interpreter-elite.html`

После того как клон `interpreter-via-l-expert.html` умеет всё из ELITE (Этап 2 закрыт и проверен на
превью), `interpreter-elite.html` — донор-исходник, больше не нужен → удалить полностью (как pro-expert).
Пока НЕ удаляем — это источник, из которого копируем слой в клон.

## Открытые развилки (уточнить у владельца по ходу)

- **Свободный via-l.** Останется ли бесплатный/App-Store велнес-вход в via-l параллельно платному
  VIA-L EXPERT? (Ось «велнес vs клиника» решается по коду доступа.) — по умолчанию ДА, велнес-вход
  остаётся, клиника включается только по коду.
- **Zoom** — только ru/uk (как в elite) или на все языки VIA-L EXPERT?
- **PRO** ($29/мес, App Store) — этой перестройки не касается, остаётся отдельно.

## Принципы

- **Удаляем по-настоящему** (файл/код/ссылки/карточки), не прячем и не комментируем (владелец).
- Живой платный продукт + вебхуки Hotmart + Apps Script → каждый шаг: бэкап (git) + синтаксис-чек +
  проверка владельцем на превью ПЕРЕД deploy/push. Клиническую механику «EXPERT/ELITE» НЕ ломать.
