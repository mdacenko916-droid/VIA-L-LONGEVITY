# Хендофф сессии 2026-07-02 (ИП: воркер N-4/N-5, велнес/юр, VIO результат)

> Прочитать первым делом завтра. Главное: **бо́льшая часть правок VIO — ЛОКАЛЬНАЯ, НЕ запушена.**

---

## ✅ СДЕЛАНО и УЖЕ В ПРОДЕ

### Воркер `interpreter/cloudflare-worker.js` (деплой `wrangler deploy`, последняя версия ~`b4b188c8`)
- **N-4 «ДВИЖЕНИЕ/НАГРУЗКА»** — пересобран под оба пола + доказательная дозировка (силовая 2–3×/нед, комбо при инсулин-теме, ходьба после еды, ВИИТ, тайминг сна); формат «Движение» = проблема→решение→доза; силовые для 40+ как императив.
- **N-5 «ПИТАНИЕ/НУТРИЦИЯ»** — новый модуль (оба пола): вес→гормоны/обмен как главный рычаг, белок 1.0–1.5 г/кг в связке с силовыми 40+, клетчатка, адресно магний/фитоэстрогены (жен) и жиры/цинк/D при дефиците (муж), алкоголь/сахар; формат «Питание» = проблема→решение→рекомендация.
- **wellnessGuard**: VIO/PRO — расширены велнес-замены (движение/обмен/эстрон/SHBG/ароматаза/HOMA-IR → баланс; питание/дозировку как образ жизни разрешено); EXPERT/ELITE — добавлена ЮР-РАМКА (EU MDR 2017/745 / FTC / FDA / Hotmart 4.11): клинический язык ок, но БЕЗ claims «лечит/излечивает/предотвращает», + образовательный дисклеймер. CORS воркера = `*` (готов к изолированному приложению).

### HTML на via-l.com (GitHub Pages, `git push`)
- **Единый hero** «Анализатор показателей активного образа жизни» / EN «Metrics Analyzer for an Active Lifestyle» — **12 языков**, в PRO/EXPERT/ELITE (+ починен застрявший словарь PRO). Коммит `716b1d0`.
- **Бейдж Perry Academy** возвращён в EXPERT/ELITE (`3b5522f`). VIO/PRO — намеренно без бейджа (App Store).
- **Сайт index.html**: блок «Анализ данных» смягчён (`cdf0edf`); **FDA/DSHEA-дисклеймер в футер** (`0f6d2a9`).
- Воркер закоммичен (`0f91237`).

### Приложение `app/` — ИЗОЛЯЦИЯ (вариант 1), закоммичено/запушено (`b9654ba`)
- `capacitor.config.json` — убран `server.url` на via-l.com (приложение грузит `www` локально; store не видит домен; уходит риск Apple 4.2).
- `app/sync-web.sh` — собирает `www` из веб-ИП (запускать перед каждой сборкой). `www/` в .gitignore.
- README обновлён. **Нативная сборка/тест — на Mac, когда веб заморожен.**

---

## ⚠️ СДЕЛАНО, но НЕ ЗАПУШЕНО (ЛОКАЛЬНО в `interpreter/interpreter-vio.html`)

**VIO — живой платный (€9.90). Всё ниже протестировано в VSCode, синтаксис валиден (vm.Script), бэкапы в scratchpad. НО в прод НЕ выложено — завтра решить: коммит+push или ещё тест.**

1. **4 ядровые care-карточки** (Сон · Стресс · Питание · Вес/Сила): формат «маркер + короткий якорь» (не абзац), велнес-язык, **12 языков**, **always-on** — 3 состояния: проблема / норма («всё хорошо») / нет данных («Расскажите…»). Глубину даёт ИИ-разбор.
2. **`recSections` убран** — дедуп с ИИ (списки питание/добавки больше не дублируются; `recs` ещё уходят в PDF).
3. **Старый пошаговый режим ПОЛНОСТЬЮ удалён**: `goTo` упрощён (только лента + результат), 15 `.btn-row` (29 кнопок ←/Далее), `goFromProfile()`, прогресс-бар шагов (`pd0…pd12`) — всё вычищено. `STEP_CADENCE` НЕ трогать (живой — группирует ленту).
4. **«Пройти заново»** (`restart`) чистит `vial_steplast` → полная форма (все каденс-блоки снова «пора»).
5. **EN-i18n**: `setLang` теперь пере-рендерит ленту при смене языка (заголовки групп `_flowHead` синхронятся с контентом). Владелец подтвердил, что перевод ок.
6. **«Баланс сил»**: мед-фраза «…гормонального дисбаланса» → велнес (12 языков).
7. **Чистка модулей результата от мед-терминов** (~350 вхождений):
   - УБРАНЫ (клинические дубли ИИ): `renderNutriCorr` (Нутри-корреляции), `renderProtocol24` (Протокол 24ч), «Питание по времени» (в Циркадном). Функции закомментированы в `renderProModules`, контейнеры `mod-nutri`/`mod-protocol` спрятаны.
   - ДЕ-МЕДИКАЛИЗОВАНЫ: Циркадный таймлайн (доза магния → «магниевые продукты»); «Гормональный трекер фазы» → **«Трекер фазы и витальности»** (12 языков + сабтайтл «гормональный контекст» → «контекст фазы»).

---

## 🌅 НА ЗАВТРА (не доделано)

1. **🔴 КОММИТ + PUSH VIO** (`interpreter-vio.html`) — всё из блока ⚠️ локально. После финального теста владельца → `git push` (via-l.com).
2. **ИИ-разбор воркера в VIO — дозы/термины.** В результате ИИ-разбор всё ещё пишет «магний глицинат 300–400 мг», «гормональный фон», «микробиом» (гайрдрейл сейчас разрешает дозы как «образ жизни»). Решить: убирать ли конкретные мг-дозы/термины из VIO-ИИ → правка `wellnessGuard` воркера + деплой.
3. **Модуль «Рекомендованные показатели баланса»** — ещё «Ферритин», «Витамин D», «Мужская витальность». Де-медикализовать?
4. **Мелочь Циркадного**: low-energy вариант (score<40), окно 18–21 «Магний, ромашка» — не поправлено (без дозы, borderline).
5. **Перезапустить результат в VSCode** — глазами найти оставшиеся мед-термины (владелец так тестирует).
6. **Паритет?** — перенести ли care-карточки / чистку модулей в PRO/EXPERT/ELITE (у них свои страницы результата). EXPERT/ELITE = клиника (дозы ок), так что скорее только VIO/PRO.
7. **Hotmart ручное** (`docs/HOTMART-COMPLIANCE-BRIEF.md`): локализовать дисклеймер (ES/UK/RU), заменить «Lorem ipsum» в описаниях, статус авторов «коуч». Чекауты уже с EN-дисклеймером ✓.
8. **Приложение**: собрать/протестировать на Mac (вариант 1) — когда веб заморожен.

---

## Как тестируется / настроено
- VIO/веб-ИП тестируется в **VSCode Live Preview** (локально, без деплоя). Сброс данных: DevTools → Application → Local Storage → Clear + жёсткий reload.
- Синтакс-чек inline-JS: `node` + `vm.Script` по всем `<script>` (паттерн «аккуратненько»).
- Деплой: HTML → `git push origin main` (GitHub Pages, via-l.com); воркер → `cd interpreter && wrangler deploy`.
- Бэкапы правок — в scratchpad сессии.
