# Типографический канон VIA-L / VIA-L EXPERT

**Дата:** 2026-08-24. **Причина:** аудитория 40+ — возрастная дальнозоркость почти у всех.
До канона в `interpreter-via-l.html` было **38 разных размеров** шрифта, из них **227 вхождений
мельче 13px**. На телефоне это нечитаемо, а мелкий текст в приложении о здоровье читается ещё и
как «нам всё равно, прочтёте вы это или нет».

## Шкала — четыре ступени, других не заводить

Шкала **адаптивная**: на телефоне вся она поднимается на ступень. Опорная точка — **17px, системный
размер body в iOS**; именно он с 2026-07 стоит у текста разбора как отдельное решение по читаемости
для 45+ (`@media (max-width:599px)`, там же шрифт переключается на SF Pro и вес поднимается с 300 до
400). Канон подчинён этому числу, а не наоборот: `--fs-body` на мобильном = те же 17px.

| Токен | Десктоп | Телефон (≤599px) | Для чего |
|---|---|---|---|
| `--fs-cap` | 13px | 14px | подписи, единицы, служебные строки. **Нижняя граница: мельче не делаем нигде** |
| `--fs-body` | 15px | **17px** | основной текст: разбор, строки данных, абзацы, поля ввода |
| `--fs-title` | 18px | 20px | заголовки секций и карточек |
| `--fs-hero` | 24px | 26px | имя клиента, крупные значения, акцентные числа |

Токены объявлены в `:root` обоих тарифов. В новом коде писать `font-size: var(--fs-body)`,
а не пиксели.

**Исключение — дисплейные цифры.** Индекс дня и подобные крупные числа (40–56px) под шкалу не
подпадают: это графика, а не текст.

## Семейства — два, плюс системный fallback

- **Playfair Display** — только заголовки (и логотип).
- **`var(--font-ui)`** — весь остальной текст. На телефоне подменяется на системный SF Pro:
  он шире и с большим x-height, чем DM Sans, поэтому разбор читается заметно легче.
- `Oswald` — историческое исключение для заголовков секций карточки (`.vc-tt`).
- `monospace` — только служебный вывод, в клиентском тексте не появляется.

## Правило перевода старых размеров

При переводе экрана на канон: `≤12.5px → --fs-cap`, `13–14px → --fs-body`,
`14.5–19px → --fs-title`, `20–26px → --fs-hero`, крупнее — оставить как есть.
Заголовок секции переводится в `--fs-title`, даже если раньше был 13px: ступень выбирается
по РОЛИ элемента, а не только по прежнему размеру.

## Что под канон НЕ подпадает

- **Иконки** (`.ph`, `*-ic`, шевроны, стрелки, флаги, `::before`-глифы): их `font-size` — это размер
  рисунка, а не текста. Оставлены в пикселях.
- **Нижняя навигация**: четыре таба в ряд, подписи обязаны держаться в одну строку. Под общую шкалу
  не идёт, но 8–8.5px для 40+ нечитаемы, поэтому поднята до 11px (12px на широком экране) со снятой
  разрядкой — разрядка и была тем, что не давало увеличить кегль.
- **Дисплейные цифры** 20px и крупнее (индекс дня, значения плиток, заголовки экранов): читаемы и
  без канона, трогать их — значит ломать вёрстку ради нуля пользы.

## Статус перевода


- ✅ **Текст разбора** (`.ai-intro`, `#aiResult`, `.ai-sec-h`) — переведён на токены, значение не изменилось (было 17/18.5, стало `--fs-body`/`--fs-title`).
- ✅ **Карточка «Мой профиль»** (`_vcCSS` + inline в `renderCard`) — оба тарифа, 2026-08-24.
- ✅ **Блок «Анализы»** (`_labsInner`) + раскладка строки маркера под телефон — 2026-08-24.
- ✅ **Весь остальной интерфейс обоих тарифов** — 2026-08-24: 246 CSS-правил и 120–140 inline-стилей
  переведены на токены. Осталось в пикселях только то, что перечислено выше.
- ✅ **Кабинет специалиста** (`cabinet/index.html`), **витрина** (`interpreter/my-specialist.html`),
  **анкета** (`book/anketa/index.html`) — 2026-08-24: те же токены в `:root`, тот же мобильный блок,
  тот же перевод правил и inline-стилей. Светлые темы витрины и кабинета при этом не тронуты.
- ✅ **Лендинги сайта** (`index.html`, четыре `*-program.html`) — 2026-08-24, но по ОСОБОМУ правилу:
  общая шкала туда не тащится. На лендингах выверенная дизайн-иерархия и много графики, где
  `font-size` задаёт масштаб рисунка: корешки и обложки книг, бейджи на фото, логотипы. Поэтому
  поднят только «пол читаемости» — текстовые правила 10–12.9px переведены в 13px, а всё мельче 10px
  (заведомо графика) оставлено как есть.
- ⚠️ Проверять глазами на телефоне: подъём кегля мог сузить место в плотных сетках (чипы ответов,
  плитки метрик, таблицы). Правится ступенью конкретного элемента, а не шкалой.
